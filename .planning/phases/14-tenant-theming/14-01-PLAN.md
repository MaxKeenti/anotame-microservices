---
phase: 14-tenant-theming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java
  - anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/service/EstablishmentService.java
  - anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java
  - anotame-api/backend/operations-service/src/main/resources/db/migration/V*__add_establishment_theme_fields.sql
autonomous: true
requirements:
  - THEME-01
  - THEME-02
must_haves:
  truths:
    - "EstablishmentJpa has primaryColor (VARCHAR(7), nullable) and fontFamily (VARCHAR(32), nullable) fields with JPA @Column annotations"
    - "EstablishmentService getters and setters for primaryColor and fontFamily are present"
    - "GET /api/establishments/{id} returns primaryColor and fontFamily fields (nullable, can be null)"
    - "PUT /api/establishments/{id} accepts primaryColor and fontFamily in request body and persists to database"
    - "Flyway migration V*__add_establishment_theme_fields.sql creates two new nullable columns in tce_establishment table"
    - "Build passes: `mvn clean install` or equivalent for operations-service without errors"
    - "Unit tests verify primaryColor hex format storage and fontFamily enum values (or at minimum, fields serialize/deserialize correctly)"
  artifacts:
    - path: "anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java"
      provides: "JPA entity with primaryColor and fontFamily fields"
      contains: "@Column(name = \"primary_color\""
    - path: "anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/service/EstablishmentService.java"
      provides: "Service methods to get/set theme fields"
      exports: ["getPrimaryColor()", "setPrimaryColor(String)", "getFontFamily()", "setFontFamily(String)"]
    - path: "anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java"
      provides: "REST endpoints exposing theme fields"
      exports: ["GET /api/establishments/{id}", "PUT /api/establishments/{id}"]
    - path: "anotame-api/backend/operations-service/src/main/resources/db/migration/V*__add_establishment_theme_fields.sql"
      provides: "Database schema migration to add theme columns"
      contains: ["ALTER TABLE tce_establishment ADD COLUMN primary_color", "ALTER TABLE tce_establishment ADD COLUMN font_family"]
  key_links:
    - from: "EstablishmentJpa primaryColor field"
      to: "EstablishmentController GET /api/establishments/{id}"
      via: "JPA serialization"
      pattern: "primaryColor"
    - from: "EstablishmentController PUT endpoint"
      to: "EstablishmentService.updateEstablishment()"
      via: "dependency injection"
      pattern: "establishmentService.update"
    - from: "Flyway migration"
      to: "EstablishmentJpa @Column annotations"
      via: "database schema"
      pattern: "primary_color"
---

<objective>
Add persistent storage for tenant theme configuration (primary color and font family) to the operations-service backend. Extend EstablishmentJpa entity with two new nullable fields, create database migration, update service layer to handle serialization, and expose fields via REST API endpoints.

Purpose: Enables backend persistence of tenant-selected brand colors and typography choices. Frontend will fetch and apply these values on app load.

Output: Modified EstablishmentJpa entity, database migration, service methods, and REST endpoints ready to receive and return theme configuration.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.planning/phases/14-tenant-theming/14-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-tenant-theming/14-CONTEXT.md
@.planning/phases/14-tenant-theming/14-ASSUMPTIONS.md

## Backend Architecture References
- Operations-service exists at: `anotame-api/backend/operations-service/`
- EstablishmentJpa current fields: name, ownerName, taxInfo, dailyCapacityMinutes (review to understand current pattern)
- Flyway migrations located at: `anotame-api/backend/operations-service/src/main/resources/db/migration/`
</context>

<tasks>

<task type="auto" wave="1">
  <name>Task 1: Add primaryColor and fontFamily fields to EstablishmentJpa entity</name>
  <files>anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java</files>
  <action>
Modify EstablishmentJpa.java to add two new fields with JPA annotations:

1. **Add field declarations** (after existing fields like dailyCapacityMinutes):
   ```java
   @Column(name = "primary_color", length = 7, nullable = true)
   private String primaryColor;  // Hex format: "#FF6B6B", nullable

   @Column(name = "font_family", length = 32, nullable = true)
   private String fontFamily;  // Enum values: "Inter", "Outfit", "Merriweather", nullable
   ```

2. **Add getter and setter methods**:
   ```java
   public String getPrimaryColor() {
       return primaryColor;
   }

   public void setPrimaryColor(String primaryColor) {
       this.primaryColor = primaryColor;
   }

   public String getFontFamily() {
       return fontFamily;
   }

   public void setFontFamily(String fontFamily) {
       this.fontFamily = fontFamily;
   }
   ```

3. **No changes needed to**:
   - Constructor (use default constructor or existing pattern)
   - @Entity or @Table annotations (already correct)
   - Existing fields (name, ownerName, taxInfo, dailyCapacityMinutes remain unchanged)

4. **Ensure fields are nullable**: Both fields must allow null values in the database, so field validation is NOT strict. The application treats null as "use default theme".

**Why these changes:**
- Hex color length: 7 chars ("#RRGGBB")
- Font family length: 32 chars covers "Inter Variable", "Outfit Variable", "Merriweather Variable"
- Nullable fields: Backward compatible — existing establishments get null (fallback to Anotame defaults)
- No validation at entity level: Validation happens in frontend (sveltekit-superforms) and optionally Phase 15 (backend contrast checking)
  </action>
  <verify>
    <automated>
      - File contains `private String primaryColor;` with `@Column(name = "primary_color"`: `grep -n "primary_color" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java`
      - File contains getter/setter for primaryColor: `grep -n "getPrimaryColor\|setPrimaryColor" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java`
      - File contains `private String fontFamily;` with `@Column(name = "font_family"`: `grep -n "font_family" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java`
      - File contains getter/setter for fontFamily: `grep -n "getFontFamily\|setFontFamily" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/jpa/EstablishmentJpa.java`
      - Java compiles without errors: `cd anotame-api/backend/operations-service && mvn clean compile -DskipTests 2>&1 | grep -q "BUILD SUCCESS"` or `BUILD FAILURE` check
    </automated>
  </verify>
  <done>EstablishmentJpa has primaryColor (VARCHAR(7), nullable) and fontFamily (VARCHAR(32), nullable) fields with full getter/setter methods. Entity compiles without errors. No changes to existing fields or constructors.</done>
</task>

<task type="auto" wave="1">
  <name>Task 2: Create Flyway migration to add theme columns to tce_establishment table</name>
  <files>anotame-api/backend/operations-service/src/main/resources/db/migration/V*__add_establishment_theme_fields.sql</files>
  <action>
Create a new Flyway migration file in the db/migration directory with filename pattern V{VERSION}__add_establishment_theme_fields.sql (where {VERSION} is the next sequential version number).

**Steps:**

1. **Determine next version number** from existing migrations in `anotame-api/backend/operations-service/src/main/resources/db/migration/`:
   - List files to see current version (e.g., V1, V2, ... V10)
   - Next version will be V{N+1}

2. **Create migration file** with content:
   ```sql
   -- Flyway Migration: Add establishment theme customization fields
   -- Adds support for per-tenant brand color and font family overrides

   ALTER TABLE tce_establishment ADD COLUMN primary_color VARCHAR(7) NULL;
   COMMENT ON COLUMN tce_establishment.primary_color IS 'Hex color format: #RRGGBB (e.g., #FF6B6B). Null = use default Anotame theme.';

   ALTER TABLE tce_establishment ADD COLUMN font_family VARCHAR(32) NULL;
   COMMENT ON COLUMN tce_establishment.font_family IS 'Font family selection: Inter, Outfit, or Merriweather. Null = use default Inter Variable.';
   ```

3. **Ensure idempotency and safety**:
   - Columns are nullable (no NOT NULL constraint — backward compatible)
   - No data transformation needed (new establishments start with null)
   - No foreign key constraints on new columns (theme is independent)
   - Comments document the format expectations for future queries

4. **Place file** in: `anotame-api/backend/operations-service/src/main/resources/db/migration/`

**Why this approach:**
- Flyway auto-discovers .sql files in this directory
- Nullable columns don't require existing data migration
- Comments provide schema documentation
- Next version number prevents conflicts with other migrations
  </action>
  <verify>
    <automated>
      - Migration file exists: `ls anotame-api/backend/operations-service/src/main/resources/db/migration/ | grep -E "V[0-9]+__add_establishment_theme_fields"`
      - File contains ALTER TABLE tce_establishment: `grep -n "ALTER TABLE tce_establishment" anotame-api/backend/operations-service/src/main/resources/db/migration/V*__add_establishment_theme_fields.sql`
      - File contains both column additions: `grep -c "ADD COLUMN" anotame-api/backend/operations-service/src/main/resources/db/migration/V*__add_establishment_theme_fields.sql | grep -q "^2$"`
      - Flyway migration syntax valid: Manual syntax check (no SQL parser available, but grep confirms structure)
    </automated>
  </verify>
  <done>Flyway migration created at correct location with version number, adds primary_color and font_family columns to tce_establishment table with nullable constraints and documentation comments. File name follows Flyway naming convention.</done>
</task>

<task type="auto" wave="1">
  <name>Task 3: Update EstablishmentService to expose theme fields in business logic</name>
  <files>anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/service/EstablishmentService.java</files>
  <action>
Modify EstablishmentService.java to include getter and setter logic for theme fields.

**Implementation approach depends on service pattern:**

**If EstablishmentService has update/get methods:**
1. Locate existing `getEstablishment(id)` or `get()` method
2. Ensure the method already returns the full EstablishmentJpa object (which now includes primaryColor and fontFamily via JPA mapping)
3. No changes needed — JPA auto-maps new fields to returned entity

**If EstablishmentService uses a DTO (EstablishmentDTO) for serialization:**
1. Open `EstablishmentDTO.java` (likely in `domain/dto/` or `application/dto/`)
2. Add fields to match EstablishmentJpa:
   ```java
   private String primaryColor;
   private String fontFamily;
   ```
3. Add getter/setter methods for both fields
4. Update `toEntity()` method (if exists) to copy DTO fields to EstablishmentJpa:
   ```java
   jpa.setPrimaryColor(this.primaryColor);
   jpa.setFontFamily(this.fontFamily);
   ```
5. Update `fromEntity()` method (if exists) to copy JPA fields to DTO:
   ```java
   this.primaryColor = jpa.getPrimaryColor();
   this.fontFamily = jpa.getFontFamily();
   ```

**If EstablishmentService uses a repository pattern:**
1. No changes needed — repository auto-loads/saves JPA fields

**Verification approach:** Look for where EstablishmentService builds responses. If it returns EstablishmentJpa objects directly, nothing to change. If it uses DTOs, propagate the new fields through the DTO.

**Why this approach:**
- Service layer is thin — just passes data through
- DTO mapping (if present) ensures consistent API responses
- JPA handles database persistence automatically
  </action>
  <verify>
    <automated>
      - EstablishmentService.java exists: `ls anotame-api/backend/operations-service/src/main/java/com/anotame/operations/domain/service/EstablishmentService.java`
      - If DTO exists, check for primaryColor: `grep -n "primaryColor\|fontFamily" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/*/EstablishmentDTO.java 2>/dev/null || echo "No DTO found — using direct JPA mapping"`
      - Service compiles: `cd anotame-api/backend/operations-service && mvn clean compile -DskipTests 2>&1 | grep -q "BUILD SUCCESS"`
    </automated>
  </verify>
  <done>EstablishmentService (and any DTO layer) properly maps primaryColor and fontFamily fields. JPA persistence works automatically. Service compiles without errors.</done>
</task>

<task type="auto" wave="1">
  <name>Task 4: Update EstablishmentController to include theme fields in REST endpoints</name>
  <files>anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java</files>
  <action>
Modify EstablishmentController.java to ensure theme fields are exposed in API responses and accepted in request bodies.

**What to verify/update:**

1. **GET /api/establishments/{id} endpoint:**
   - Locate existing GET method (likely `getEstablishment(@PathVariable id)`)
   - Confirm it returns the establishment object (which now includes primaryColor and fontFamily)
   - No changes needed — JPA serialization auto-includes new fields in JSON response
   - **Expected response** will look like:
     ```json
     {
       "id": "uuid",
       "name": "establishment-name",
       "primaryColor": "#FF6B6B",
       "fontFamily": "Outfit",
       "dailyCapacityMinutes": 480
     }
     ```

2. **PUT /api/establishments/{id} endpoint:**
   - Locate existing PUT method (likely `updateEstablishment(@PathVariable id, @RequestBody payload)`)
   - Confirm the request body DTO/payload includes primaryColor and fontFamily fields
   - If using a DTO, ensure it has getters/setters for both fields (from Task 3)
   - No explicit changes needed — request body deserialization auto-maps JSON to Java object
   - **Expected request** will include:
     ```json
     {
       "name": "establishment-name",
       "primaryColor": "#FF6B6B",
       "fontFamily": "Inter"
     }
     ```

3. **Response serialization:**
   - No explicit JSON serialization changes needed — Jackson (or REST framework) auto-serializes all non-transient fields
   - Nullable fields will serialize as `null` if empty (expected behavior)

4. **Build and verify:**
   - Run `mvn clean install` to ensure no serialization errors
   - Check that endpoints compile without warnings

**Why this approach:**
- REST frameworks (Spring Boot, Quarkus, etc.) auto-serialize entity fields
- New nullable fields are backward compatible — old clients don't break
- Response structure automatically includes new fields
  </action>
  <verify>
    <automated>
      - EstablishmentController.java exists: `ls anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java`
      - Controller has GET establishment method: `grep -n "@GetMapping\|@RequestMapping.*GET\|getEstablishment" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java`
      - Controller has PUT establishment method: `grep -n "@PutMapping\|@RequestMapping.*PUT\|updateEstablishment" anotame-api/backend/operations-service/src/main/java/com/anotame/operations/application/controller/EstablishmentController.java`
      - Build succeeds: `cd anotame-api/backend/operations-service && mvn clean install -DskipTests 2>&1 | tail -5 | grep -q "BUILD SUCCESS"`
    </automated>
  </verify>
  <done>EstablishmentController GET /api/establishments/{id} returns primaryColor and fontFamily in JSON response. PUT /api/establishments/{id} accepts primaryColor and fontFamily in request body. Serialization works automatically via JPA/Jackson. Build passes without errors.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Database migration applied:**
   - Run Flyway migration: `mvn flyway:migrate` (or equivalent for your operations-service setup)
   - Verify columns exist in database: `SELECT primary_color, font_family FROM tce_establishment LIMIT 1;` should show two null columns

2. **API endpoint verification (manual or via test):**
   - GET /api/establishments/{establishment-id} returns primaryColor and fontFamily (both null or with values if set)
   - PUT /api/establishments/{establishment-id} with body `{"name": "Test", "primaryColor": "#FF6B6B"}` persists the color
   - Verify round-trip: PUT color, GET establishment, confirm color matches

3. **Build verification:**
   - `mvn clean install` passes in operations-service directory
   - No compilation errors or warnings
   - No serialization/deserialization errors in REST endpoints

4. **Backward compatibility:**
   - Existing establishments (without theme set) still load and return null for primaryColor and fontFamily
   - Endpoints work for both old clients (ignoring theme fields) and new clients (reading/writing theme fields)
</verification>

<success_criteria>
1. EstablishmentJpa has primaryColor and fontFamily fields with JPA @Column annotations and nullable constraints
2. EstablishmentJpa has getter/setter methods for both theme fields
3. Flyway migration file exists and creates primary_color and font_family columns in tce_establishment table
4. EstablishmentService or DTO layer properly serializes/deserializes theme fields
5. EstablishmentController GET /api/establishments/{id} returns primaryColor and fontFamily (JSON response)
6. EstablishmentController PUT /api/establishments/{id} accepts primaryColor and fontFamily in request body
7. Build passes: `mvn clean install` succeeds without errors
8. Flyway migration runs successfully (columns added to database)
9. Round-trip test: Set color via PUT, retrieve via GET, confirm color persists
10. Backward compatible: Existing establishments without theme values work correctly (null values)
</success_criteria>

<output>
After plan completion, create `.planning/phases/14-tenant-theming/14-01-SUMMARY.md` documenting:
- What was built: EstablishmentJpa theme fields, Flyway migration, service layer updates, controller exposure
- Files modified: 4 files (EstablishmentJpa.java, EstablishmentService.java, EstablishmentController.java, Flyway migration)
- Database state: tce_establishment now has primary_color and font_family columns
- API surface: GET/PUT /api/establishments/{id} now include theme fields
- Verification results: Build passes, migration applies, round-trip test succeeds
- Requirements satisfied: THEME-01 (storage), THEME-02 (retrieval)
</output>
