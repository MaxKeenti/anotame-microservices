---
phase: 07-operational-reliability-housekeeping
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - docker-compose.yml
autonomous: false
requirements:
  - OPS-01

must_haves:
  truths:
    - "docker-compose.yml has healthcheck entries for all 4 backend services using /q/health/ready"
    - "sales-service depends_on identity-service and catalog-service with condition: service_healthy"
    - "operations-service depends_on sales-service with condition: service_healthy"
    - "anotame-web depends_on identity-service, catalog-service, sales-service with condition: service_healthy"
    - "All 4 backend containers show (healthy) status after docker compose up --build"
  artifacts:
    - path: "docker-compose.yml"
      provides: "healthcheck blocks for all 4 backend services + upgraded depends_on conditions"
      contains: "q/health/ready"
  key_links:
    - from: "docker-compose.yml healthcheck"
      to: "/q/health/ready"
      via: "wget -q --spider"
      pattern: "wget.*q/health/ready"
    - from: "sales-service depends_on"
      to: "identity-service, catalog-service"
      via: "condition: service_healthy"
      pattern: "service_healthy"
---

<objective>
Wire Docker Compose health checks for all 4 backend services using `wget --spider` against
`/q/health/ready`, and upgrade `depends_on` conditions from `service_started` to `service_healthy`
for all backend-to-backend dependencies.

Purpose: Ensures dependent services only start after their dependencies are truly ready (DB connected
and Quarkus serving HTTP), eliminating race condition failures on `docker compose up`.

Output: Updated `docker-compose.yml` with healthcheck blocks for identity, catalog, sales, and
operations services; upgraded depends_on conditions throughout.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-operational-reliability-housekeeping/07-RESEARCH.md
@.planning/phases/07-operational-reliability-housekeeping/07-01-SUMMARY.md

<interfaces>
<!-- Current docker-compose.yml state (read before editing) -->

Current service ports (confirmed from docker-compose.yml + application.properties):
  identity-service  → port 8081
  catalog-service   → port 8082
  sales-service     → port 8083
  operations-service → port 8084

Current depends_on state that must be upgraded:
  sales-service depends on:
    anotame-db       → condition: service_healthy (already correct)
    identity-service → condition: service_started (UPGRADE to service_healthy)
    catalog-service  → condition: service_started (UPGRADE to service_healthy)
  operations-service depends on:
    anotame-db       → condition: service_healthy (already correct)
    sales-service    → condition: service_started (UPGRADE to service_healthy)
  anotame-web depends on:
    identity-service, catalog-service, sales-service — list form, no condition (UPGRADE to long-form with service_healthy)

Healthcheck block to add to each backend service (use exact ports):

identity-service (port 8081):
```yaml
healthcheck:
  test: ["CMD-SHELL", "wget -q --spider http://localhost:8081/q/health/ready || exit 1"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

catalog-service (port 8082):
```yaml
healthcheck:
  test: ["CMD-SHELL", "wget -q --spider http://localhost:8082/q/health/ready || exit 1"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

sales-service (port 8083):
```yaml
healthcheck:
  test: ["CMD-SHELL", "wget -q --spider http://localhost:8083/q/health/ready || exit 1"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

operations-service (port 8084):
```yaml
healthcheck:
  test: ["CMD-SHELL", "wget -q --spider http://localhost:8084/q/health/ready || exit 1"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

Target depends_on for sales-service:
```yaml
depends_on:
  anotame-db:
    condition: service_healthy
  identity-service:
    condition: service_healthy
  catalog-service:
    condition: service_healthy
```

Target depends_on for operations-service:
```yaml
depends_on:
  anotame-db:
    condition: service_healthy
  sales-service:
    condition: service_healthy
```

Target depends_on for anotame-web (upgrade from simple list):
```yaml
depends_on:
  identity-service:
    condition: service_healthy
  catalog-service:
    condition: service_healthy
  sales-service:
    condition: service_healthy
```

Do NOT add a healthcheck to anotame-db — it already has one (`pg_isready`).
Do NOT add a healthcheck to anotame-pgadmin — GUI tool, no health requirement.
Do NOT add a healthcheck to anotame-web — frontend, not a dependency target.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add healthcheck blocks and upgrade depends_on conditions in docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (read full file before editing — required to preserve all existing structure)
  </read_first>
  <action>
    Edit docker-compose.yml to add healthcheck blocks to all 4 backend services and upgrade
    depends_on conditions. Make the following changes:

    1. Add healthcheck to identity-service (after networks: block, before or after depends_on):
    ```yaml
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8081/q/health/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    ```

    2. Add healthcheck to catalog-service:
    ```yaml
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8082/q/health/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    ```

    3. Add healthcheck to sales-service:
    ```yaml
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8083/q/health/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    ```

    4. Add healthcheck to operations-service:
    ```yaml
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8084/q/health/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    ```

    5. Upgrade sales-service depends_on — replace the two `condition: service_started` with
       `condition: service_healthy`:
    ```yaml
    depends_on:
      anotame-db:
        condition: service_healthy
      identity-service:
        condition: service_healthy
      catalog-service:
        condition: service_healthy
    ```

    6. Upgrade operations-service depends_on — replace `condition: service_started` with
       `condition: service_healthy`:
    ```yaml
    depends_on:
      anotame-db:
        condition: service_healthy
      sales-service:
        condition: service_healthy
    ```

    7. Upgrade anotame-web depends_on — convert from list form to long-form with service_healthy:
    ```yaml
    depends_on:
      identity-service:
        condition: service_healthy
      catalog-service:
        condition: service_healthy
      sales-service:
        condition: service_healthy
    ```

    Do NOT modify anotame-db (already has healthcheck).
    Do NOT modify anotame-pgadmin.
    Do NOT add healthcheck to anotame-web.
    Preserve all existing fields: build, container_name, ports, env_file, environment, networks, volumes.
    Use consistent 2-space YAML indentation throughout.
  </action>
  <verify>
    <automated>grep -c "q/health/ready" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "q/health/ready" docker-compose.yml` returns 4 (one per backend service)
    - `grep -c "service_started" docker-compose.yml` returns 0 (all upgraded to service_healthy)
    - `grep "wget.*8081.*q/health/ready" docker-compose.yml` exits 0
    - `grep "wget.*8082.*q/health/ready" docker-compose.yml` exits 0
    - `grep "wget.*8083.*q/health/ready" docker-compose.yml` exits 0
    - `grep "wget.*8084.*q/health/ready" docker-compose.yml` exits 0
    - `grep -c "service_healthy" docker-compose.yml` returns at least 9 (anotame-db×4, identity×2, catalog×2, sales×2, anotame-web deps×3)
    - `docker compose config` exits 0 (YAML is valid)
  </acceptance_criteria>
  <done>docker-compose.yml has healthcheck blocks on all 4 backend services; all depends_on conditions use service_healthy; YAML validates cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 4 backend services have healthcheck entries in docker-compose.yml and all backend-to-backend
    depends_on conditions use service_healthy. Plan 01 added quarkus-smallrye-health to all 4 pom.xml files.
  </what-built>
  <how-to-verify>
    Run the full stack and verify all containers reach healthy status:

    ```bash
    docker compose down -v
    docker compose up --build -d
    sleep 45
    docker ps --format "table {{.Names}}\t{{.Status}}"
    ```

    Expected: All 4 backend containers show `(healthy)` in the Status column.
    Then verify the health endpoints directly:

    ```bash
    curl -s http://localhost:8081/q/health/ready
    curl -s http://localhost:8082/q/health/ready
    curl -s http://localhost:8083/q/health/ready
    curl -s http://localhost:8084/q/health/ready
    ```

    Expected: Each returns `{"status":"UP","checks":[{"name":"Database connections health check","status":"UP"}]}`
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 containers are healthy and all 4 health endpoints return status UP. Describe any failures.</resume-signal>
</task>

</tasks>

<verification>
File assertions (run before human verify):
```bash
# Count health endpoint references — must be 4
grep -c "q/health/ready" docker-compose.yml

# Confirm no service_started remains
grep "service_started" docker-compose.yml && echo "ERROR: service_started found" || echo "OK: all upgraded"

# YAML validity check
docker compose config > /dev/null && echo "YAML valid" || echo "YAML invalid"
```
</verification>

<success_criteria>
- docker-compose.yml contains exactly 4 occurrences of `/q/health/ready`
- Zero occurrences of `condition: service_started` remain
- `docker compose config` exits 0
- After `docker compose up --build`, all 4 backend containers show `(healthy)` in `docker ps`
- Each of ports 8081–8084 returns HTTP 200 at `/q/health/ready` with `"status":"UP"`
</success_criteria>

<output>
After completion, create `.planning/phases/07-operational-reliability-housekeeping/07-02-SUMMARY.md`
</output>
