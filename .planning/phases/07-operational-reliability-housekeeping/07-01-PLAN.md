---
phase: 07-operational-reliability-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-api/backend/identity-service/pom.xml
  - anotame-api/backend/catalog-service/pom.xml
  - anotame-api/backend/sales-service/pom.xml
  - anotame-api/backend/operations-service/pom.xml
autonomous: true
requirements:
  - OPS-02

must_haves:
  truths:
    - "All 4 backend services expose /q/health/ready when started"
    - "DB connectivity is checked automatically via Agroal integration — no custom Java code written"
    - "operations-service does NOT gain quarkus-smallrye-jwt (it was not there before)"
  artifacts:
    - path: "anotame-api/backend/identity-service/pom.xml"
      provides: "quarkus-smallrye-health dependency"
      contains: "quarkus-smallrye-health"
    - path: "anotame-api/backend/catalog-service/pom.xml"
      provides: "quarkus-smallrye-health dependency"
      contains: "quarkus-smallrye-health"
    - path: "anotame-api/backend/sales-service/pom.xml"
      provides: "quarkus-smallrye-health dependency"
      contains: "quarkus-smallrye-health"
    - path: "anotame-api/backend/operations-service/pom.xml"
      provides: "quarkus-smallrye-health dependency"
      contains: "quarkus-smallrye-health"
  key_links:
    - from: "quarkus-smallrye-health"
      to: "/q/health/ready"
      via: "Agroal auto-registered DB readiness check"
      pattern: "quarkus-smallrye-health"
---

<objective>
Add the `quarkus-smallrye-health` extension to all 4 backend service pom.xml files so each service
automatically exposes `/q/health/ready`, `/q/health/live`, and `/q/health` endpoints.

Purpose: Enables Docker Compose to health-check each service using `/q/health/ready` (Plan 02),
and provides Railway/operations tooling a standardized readiness probe.

Output: 4 modified pom.xml files each containing the `quarkus-smallrye-health` dependency block.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-operational-reliability-housekeeping/07-RESEARCH.md

<interfaces>
<!-- Current dependency list shape (from identity-service/pom.xml — reference pattern for all 4) -->
<!-- All 4 services use quarkus-bom 3.27.2 — NO version tag on any quarkus dependency -->

identity-service existing quarkus dependencies (lines 31-65):
  quarkus-rest-jackson
  quarkus-hibernate-orm-panache
  quarkus-flyway
  quarkus-jdbc-postgresql
  quarkus-hibernate-validator
  quarkus-smallrye-jwt          ← identity has JWT
  quarkus-elytron-security-common
  lombok (with version 1.18.30 + scope provided)

catalog-service existing quarkus dependencies:
  quarkus-hibernate-orm-panache
  quarkus-flyway
  quarkus-jdbc-postgresql
  quarkus-hibernate-validator
  quarkus-smallrye-jwt          ← catalog has JWT

sales-service existing quarkus dependencies:
  quarkus-hibernate-orm-panache
  quarkus-flyway
  quarkus-jdbc-postgresql
  quarkus-hibernate-validator
  quarkus-smallrye-jwt          ← sales has JWT

operations-service existing quarkus dependencies:
  quarkus-hibernate-orm-panache
  quarkus-flyway
  quarkus-jdbc-postgresql
  quarkus-arc
  quarkus-hibernate-validator
  ← operations does NOT have quarkus-smallrye-jwt (pre-existing, do NOT add it)
  lombok (with version 1.18.30 + scope provided)

Dependency block to add to each service (no version — managed by quarkus-bom):
```xml
<dependency>
    <groupId>io.quarkus</groupId>
    <artifactId>quarkus-smallrye-health</artifactId>
</dependency>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quarkus-smallrye-health to identity-service and catalog-service pom.xml</name>
  <files>
    anotame-api/backend/identity-service/pom.xml
    anotame-api/backend/catalog-service/pom.xml
  </files>
  <read_first>
    - anotame-api/backend/identity-service/pom.xml (read current state before editing)
    - anotame-api/backend/catalog-service/pom.xml (read current state before editing)
  </read_first>
  <action>
    In each file, locate the `<dependencies>` block and add the following dependency after the last
    existing `quarkus-*` entry (before lombok or any non-quarkus entries):

    ```xml
    <dependency>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    ```

    No version tag — the quarkus-bom at version 3.27.2 manages the version.
    Do NOT add quarkus-smallrye-jwt or any other dependency. Add only quarkus-smallrye-health.
  </action>
  <verify>
    <automated>grep "quarkus-smallrye-health" "anotame-api/backend/identity-service/pom.xml" && grep "quarkus-smallrye-health" "anotame-api/backend/catalog-service/pom.xml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "quarkus-smallrye-health" anotame-api/backend/identity-service/pom.xml` exits 0 and prints the artifactId line
    - `grep "quarkus-smallrye-health" anotame-api/backend/catalog-service/pom.xml` exits 0 and prints the artifactId line
    - `grep "version" anotame-api/backend/identity-service/pom.xml | grep "smallrye-health"` returns empty (no version tag added)
    - `grep "version" anotame-api/backend/catalog-service/pom.xml | grep "smallrye-health"` returns empty (no version tag added)
  </acceptance_criteria>
  <done>Both identity-service and catalog-service pom.xml contain the quarkus-smallrye-health dependency block with no version tag.</done>
</task>

<task type="auto">
  <name>Task 2: Add quarkus-smallrye-health to sales-service and operations-service pom.xml</name>
  <files>
    anotame-api/backend/sales-service/pom.xml
    anotame-api/backend/operations-service/pom.xml
  </files>
  <read_first>
    - anotame-api/backend/sales-service/pom.xml (read current state before editing)
    - anotame-api/backend/operations-service/pom.xml (read current state before editing — confirm quarkus-smallrye-jwt is absent before touching it)
  </read_first>
  <action>
    In each file, locate the `<dependencies>` block and add the following dependency after the last
    existing `quarkus-*` entry (before lombok or any non-quarkus entries):

    ```xml
    <dependency>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    ```

    IMPORTANT for operations-service: This service does NOT have `quarkus-smallrye-jwt` and it
    must NOT be added. Only add `quarkus-smallrye-health`. Verify after editing that `quarkus-smallrye-jwt`
    is still absent from operations-service/pom.xml.

    No version tag on the new dependency — managed by quarkus-bom 3.27.2.
  </action>
  <verify>
    <automated>grep "quarkus-smallrye-health" "anotame-api/backend/sales-service/pom.xml" && grep "quarkus-smallrye-health" "anotame-api/backend/operations-service/pom.xml" && ! grep "quarkus-smallrye-jwt" "anotame-api/backend/operations-service/pom.xml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "quarkus-smallrye-health" anotame-api/backend/sales-service/pom.xml` exits 0 and prints the artifactId line
    - `grep "quarkus-smallrye-health" anotame-api/backend/operations-service/pom.xml` exits 0 and prints the artifactId line
    - `grep "quarkus-smallrye-jwt" anotame-api/backend/operations-service/pom.xml` returns empty / exits 1 (JWT was not added)
    - `grep "version" anotame-api/backend/operations-service/pom.xml | grep "smallrye-health"` returns empty (no version tag added)
  </acceptance_criteria>
  <done>Both sales-service and operations-service pom.xml contain quarkus-smallrye-health with no version tag; operations-service still has no quarkus-smallrye-jwt.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all 4 services have the extension:

```bash
for svc in identity-service catalog-service sales-service operations-service; do
  echo -n "$svc: "
  grep "quarkus-smallrye-health" "anotame-api/backend/$svc/pom.xml" && echo "OK" || echo "MISSING"
done
```

Confirm operations-service did not accidentally gain quarkus-smallrye-jwt:
```bash
grep "quarkus-smallrye-jwt" anotame-api/backend/operations-service/pom.xml && echo "ERROR: JWT added" || echo "OK: JWT absent"
```
</verification>

<success_criteria>
- All 4 pom.xml files contain `quarkus-smallrye-health` with no version tag
- operations-service still has no `quarkus-smallrye-jwt`
- Quarkus BOM manages the version (no drift from 3.27.2 baseline)
- `/q/health/ready` will be available on all 4 services after next Docker build
</success_criteria>

<output>
After completion, create `.planning/phases/07-operational-reliability-housekeeping/07-01-SUMMARY.md`
</output>
