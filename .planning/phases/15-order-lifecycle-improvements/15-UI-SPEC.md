---
phase: 15
slug: order-lifecycle-improvements
status: draft
shadcn_initialized: true
preset: shadcn-svelte vega / taupe / lucide
created: 2026-04-07
---

# Phase 15 — UI Design Contract

> Visual and interaction contract for Phase 15: order-lifecycle-improvements.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn-svelte |
| Preset | style: vega, baseColor: taupe |
| Component library | Radix (via shadcn-svelte) |
| Icon library | lucide-svelte |
| Font | Inter Variable (body); Outfit Variable imported but not used as primary — source: [10-01] decision |

Source: `anotame-web/components.json` + `anotame-web/src/routes/layout.css`

---

## Spacing Scale

Declared values (must be multiples of 4). All values are Tailwind CSS v4 utility classes mapped to the 8-point scale.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (p-1 / gap-1) | Icon gaps, badge padding |
| sm | 8px (p-2 / gap-2) | Compact cell padding, inline spacing between icon and label |
| md | 16px (p-4 / gap-4) | Default element spacing, form field gaps |
| lg | 24px (p-6 / gap-6) | Card padding, section padding |
| xl | 32px (p-8 / gap-8) | Layout gaps between page sections |
| 2xl | 48px (py-12) | Major section breaks |
| 3xl | 64px (py-16) | Page-level spacing (empty states) |

Exceptions:
- Touch targets: minimum `h-12` (48px height) on all interactive elements per AI_RULES.md touch-first mandate. Add `touch-manipulation` class.
- Floating action bar: `bottom-6` (24px from viewport bottom), `px-4 py-3` internal padding.
- Pickup code input: `text-2xl tracking-widest` for 6-digit display readability; no spacing exception but tracking is required.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (text-base) | 400 (font-normal) | 1.5 |
| Label / Caption | 14px (text-sm) | 400 (font-normal) | 1.5 |
| Heading | 20px (text-xl) | 600 (font-semibold) | 1.2 |
| Display | 28px (text-2xl) | 600 (font-semibold) | 1.2 |

Notes:
- Pickup code display on order detail: 28px (text-2xl), weight 600, letter-spacing wide (`tracking-widest`), monospace via `font-mono`.
- Table cell text: 14px (text-sm), weight 400.
- Floating action bar count label: 14px (text-sm), weight 600.
- All text must use Paraglide i18n — no hardcoded display strings per AI_RULES.md.

---

## Color

All values reference the existing CSS custom properties from `anotame-web/src/routes/layout.css`. Source: detected from codebase.

| Role | CSS Token | Usage |
|------|-----------|-------|
| Dominant (60%) | `--background` (oklch 1 0 0 in light) | Page background, wizard step surfaces |
| Secondary (30%) | `--card` / `--sidebar` | Cards, table rows, dialog surfaces, edit page container |
| Accent (10%) | `--primary` (oklch 0.553 0.195 38.402 — warm terracotta) | Reserved for specific elements only (see below) |
| Destructive | `--destructive` (oklch 0.577 0.245 27.325) | Destructive actions only |
| Success | `--success` (oklch 0.696 0.17 162) | DELIVERED status badge, success toasts |
| Warning | `--warning` (oklch 0.75 0.15 75) | READY status badge, pickup-code prompt note |
| Info | `--info` (oklch 0.65 0.15 250) | IN_PROGRESS status badge |

**Accent reserved for:**
1. Primary save/submit button on edit order page ("Guardar cambios")
2. "Entregar" action button on READY order rows in operations page
3. "Seleccionar" bulk mode toggle button (active state indicator border)
4. Focus ring on pickup code input field

**Not accent:** Floating action bar background uses `--card` with `--border` ring. Status badges use their own semantic tokens (success/warning/info/destructive). Edit/view icon buttons use `--muted` variant, not accent.

**Status badge color mapping:**
| Status | Color token |
|--------|------------|
| DRAFT | `--muted` / `--muted-foreground` |
| RECEIVED | `--secondary` / `--secondary-foreground` |
| IN_PROGRESS | `--info-muted` / `--info-text` |
| READY | `--warning-muted` / `--warning-text` |
| DELIVERED | `--success-muted` / `--success-text` |
| CANCELLED | `--destructive-muted` / `--destructive-text` |

---

## Component Inventory

All components below are either existing shadcn-svelte installs or project-custom components. No new third-party registries are required.

### Existing — Reused Directly

| Component | Import Path | Used For |
|-----------|-------------|----------|
| `Button` | `$lib/components/ui/button` | All CTAs, action buttons, floating bar actions |
| `Dialog` | `$lib/components/ui/dialog` | Pickup code confirmation dialog, bulk status dialog |
| `AlertDialog` | `$lib/components/ui/alert-dialog` | Bulk delete confirmation (destructive) |
| `Input` | `$lib/components/ui/input` | Pickup code entry (6-digit), search fields |
| `Tabs` | `$lib/components/ui/tabs` | "Listas para entrega" tab on operations page |
| `StatusBadge` | `$lib/components/ui/StatusBadge.svelte` | Status display in tables and order detail |
| `DataTableWrapper` | `$lib/components/ui/DataTableWrapper.svelte` | All table pages (extended with bulkActions prop) |
| `AdaptiveConfirm` | `$lib/components/ui/responsive/confirm-state.svelte` | All destructive confirmations via `adaptiveConfirm()` |
| `AdaptiveSelect` | `$lib/components/ui/responsive` | Bulk status change target selector |
| Sonner `toast` | `svelte-sonner` | All success/error notifications |
| Wizard steps | `$lib/components/orders/wizard/*` | Edit order page (reused with pre-population) |

### New — Phase 15

| Component | Path | Purpose |
|-----------|------|---------|
| `FloatingActionBar.svelte` | `$lib/components/ui/FloatingActionBar.svelte` | Floating bar for bulk actions (appears when 1+ rows selected) |
| `pickup-code-dialog.svelte` | `$lib/components/orders/pickup-code-dialog.svelte` | 6-digit code entry + DELIVER confirmation dialog |

### Modified — Phase 15

| Component | Change |
|-----------|--------|
| `DataTableWrapper.svelte` | Add optional `bulkActions?: boolean` prop; expose checkbox column + selection state |

### Icons (lucide-svelte)

| Icon | Usage |
|------|-------|
| `Check` | Confirm delivery action |
| `Pencil` / `Edit` | Edit order button (already in use) |
| `Trash2` | Delete action (already in use) |
| `CheckSquare` | Bulk select mode indicator |
| `X` | Cancel bulk mode, close floating bar |
| `ChevronDown` | Status change dropdown trigger |
| `Package` | Empty state for READY orders filter |

---

## Interaction Contracts

### Edit Order Page (`/orders/[id]/edit`)

- Page loads with wizard pre-populated from existing order data.
- OPERATOR role: garment type, service, and customer fields are rendered as read-only display text (not form inputs). Due date, notes, and payment status remain editable.
- ADMIN role: all fields editable including customer reassignment.
- DELIVERED or CANCELLED order: entire form is read-only. Display a non-dismissable `Alert` banner: "Este pedido no puede modificarse" at top of page. Edit button on order detail is hidden (not disabled) for locked statuses.
- Save button: disabled until any field has changed from the loaded value. Label: "Guardar cambios".
- On successful save: `toast.success(m.order_saved())` + navigate to `/dashboard/orders/[id]`.
- On 409 from backend: `toast.error(m.order_locked())` — do not navigate.
- On network error: `toast.error(m.generic_error())` — stay on edit page.

### Pickup Code Display (Order Detail Page)

- Pickup code shown in a dedicated card section labeled "Código de retiro".
- Display format: 6 digits with wide letter-spacing, `font-mono text-2xl font-semibold`.
- No copy-to-clipboard interaction in Phase 15 (deferred to ticket printing in Phase 17).

### Delivery Confirmation Dialog (`pickup-code-dialog.svelte`)

- Triggered by "Entregar" button on each READY row in operations page.
- Dialog title: "Confirmar entrega".
- Body: "Ingrese el código de retiro del cliente para confirmar la entrega."
- Single `<Input>` field: type="text", inputmode="numeric", maxlength="6", pattern="[0-9]{6}", `text-center text-2xl tracking-widest font-mono`.
- Submit button: "Confirmar entrega" — disabled until input is exactly 6 digits.
- On code mismatch (backend 400): display inline error below input: "Código incorrecto. Verifique con el cliente." Input is cleared, focus returned to it.
- On success: dialog closes, row transitions to DELIVERED status in UI, `toast.success(m.order_delivered())`.

### Bulk Selection (Orders Page)

- Default state: no checkboxes visible. "Seleccionar" button (secondary variant) at top-right of table header row.
- Active state: checkbox column appears as first column. "Seleccionar" button replaced by "Cancelar selección" (ghost variant). Selecting all rows via header checkbox is supported.
- Floating action bar: appears from bottom when 1+ rows are checked. `fixed bottom-6 left-1/2 -translate-x-1/2`. Bar shows: "[N] seleccionadas" + "Cambiar estado" button + "Eliminar" button (destructive color) + "×" to deselect all.
- "Cambiar estado": opens an `AdaptiveSelect` inline in the bar (OPERATOR: RECEIVED / IN_PROGRESS / READY only; ADMIN: all statuses). Confirm applies change to each selected order sequentially. Progress toast shown per batch.
- "Eliminar": only enabled if all selected orders are DRAFT status. Disabled with tooltip "Solo se pueden eliminar pedidos en borrador" otherwise. Confirmation via `adaptiveConfirm()` listing affected order ticket numbers.
- After bulk action completes: exit bulk mode, refresh table, show summary toast: "Se actualizaron [N] pedidos."

### READY Orders Tab (Operations Page)

- Existing tabs remain unchanged.
- New tab added: label "Listas para entrega" (appended after existing tabs, not replacing them).
- Tab content: DataTableWrapper filtered to `status === 'READY'` orders. Columns: Ticket, Cliente, Prendas, Entrega prometida, Acciones.
- Action column: single "Entregar" button (primary variant, `h-12 touch-manipulation`) per row.
- Empty state for this tab: heading "Sin pedidos listos", body "No hay pedidos marcados como LISTO en este momento."

---

## Copywriting Contract

All copy is in Spanish (es). Paraglide message keys must be added to `messages/es.json` and `messages/en.json`.

| Element | Copy (es) | Paraglide key |
|---------|-----------|---------------|
| Primary CTA — edit page | "Guardar cambios" | `m.order_save_changes()` |
| Edit page title | "Editar pedido #{ticketNumber}" | `m.order_edit_title()` |
| Status lock banner | "Este pedido no puede modificarse. Los pedidos entregados o cancelados son de solo lectura." | `m.order_locked_banner()` |
| Edit locked toast (409) | "No es posible editar este pedido." | `m.order_locked()` |
| Save success toast | "Pedido guardado correctamente." | `m.order_saved()` |
| Delivery dialog title | "Confirmar entrega" | `m.delivery_confirm_title()` |
| Delivery dialog body | "Ingrese el código de retiro del cliente para confirmar la entrega." | `m.delivery_confirm_body()` |
| Delivery submit button | "Confirmar entrega" | `m.delivery_confirm_action()` |
| Delivery code error | "Código incorrecto. Verifique con el cliente." | `m.delivery_code_error()` |
| Delivery success toast | "Pedido entregado correctamente." | `m.order_delivered()` |
| Pickup code section label | "Código de retiro" | `m.pickup_code_label()` |
| Bulk activate button | "Seleccionar" | `m.bulk_select_activate()` |
| Bulk cancel button | "Cancelar selección" | `m.bulk_select_cancel()` |
| Bulk count label | "{N} seleccionadas" | `m.bulk_count()` |
| Bulk change status button | "Cambiar estado" | `m.bulk_change_status()` |
| Bulk delete button | "Eliminar" | `m.bulk_delete()` |
| Bulk delete disabled tooltip | "Solo se pueden eliminar pedidos en borrador" | `m.bulk_delete_disabled()` |
| Bulk delete confirm title | "Eliminar pedidos" | `m.bulk_delete_confirm_title()` |
| Bulk delete confirm body | "Se eliminarán {N} pedidos en borrador: {ticketList}. Esta acción no se puede deshacer." | `m.bulk_delete_confirm_body()` |
| Bulk success toast | "Se actualizaron {N} pedidos." | `m.bulk_updated()` |
| READY tab label | "Listas para entrega" | `m.operations_ready_tab()` |
| READY empty state heading | "Sin pedidos listos" | `m.ready_empty_heading()` |
| READY empty state body | "No hay pedidos marcados como LISTO en este momento." | `m.ready_empty_body()` |
| Generic error toast | "Ocurrió un error. Por favor, intente nuevamente." | `m.generic_error()` |

### Destructive Actions

| Action | Trigger | Confirmation Method | Confirmation Copy |
|--------|---------|---------------------|-------------------|
| Bulk delete DRAFT orders | "Eliminar" in floating bar | `adaptiveConfirm()` | Title: "Eliminar pedidos" / Body: "Se eliminarán {N} pedidos en borrador: {list}. Esta acción no se puede deshacer." |

Note: Delivery confirmation is NOT destructive — it is a status transition requiring code validation. It uses a `Dialog` (not `AlertDialog`) with an input field, not `adaptiveConfirm()`.

### Empty States

| Context | Heading | Body |
|---------|---------|------|
| READY orders tab (operations) — no orders | "Sin pedidos listos" | "No hay pedidos marcados como LISTO en este momento." |
| Edit page — order not found | "Pedido no encontrado" | "Este pedido no existe o no tienes acceso para editarlo." |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn-svelte official | button, dialog, alert-dialog, input, tabs, form, label, textarea, select, popover, calendar, card, separator, sonner, table | not required |
| Third-party | none | not applicable |

No third-party registries are declared for Phase 15. All components are either existing project installs or project-custom Svelte components.

---

## Accessibility Notes

- Pickup code input: `aria-label="Código de retiro del cliente"`, `aria-describedby` pointing to error message element when error is shown.
- Floating action bar: `role="toolbar"`, `aria-label="Acciones en lote"`. Trap focus within bar when open.
- Bulk checkbox column header: `aria-label="Seleccionar todos"`.
- Status lock banner: `role="alert"` so screen readers announce it immediately on page load.
- All `<label>` elements chained to inputs via `for=` / `id=` pair (AI_RULES.md A11y strictness).
- Touch targets: minimum `h-12 touch-manipulation` on all interactive elements (AI_RULES.md touch-first mandate).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
