---
phase: 16-price-list-selection-in-order-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-api/backend/sales-service/src/main/resources/db/migration/V4__add_price_list_to_order.sql
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/OrderResponse.java
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderPersistenceAdapter.java
  - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java
autonomous: true
requirements:
  - pass `priceListId` through `CreateOrderRequest`
  - apply selected list to line-item pricing at creation time

must_haves:
  truths:
    - "POST /orders accepts priceListId (nullable UUID) in the request body without rejecting valid orders"
    - "A new order created with priceListId is stored in the database with that value in the price_list_id column"
    - "GET /orders/{id} returns priceListId and priceListName in the response for display in the edit wizard"
    - "Existing orders without priceListId are unaffected — the migration is additive and nullable"
    - "UpdateOrderRequest does NOT contain priceListId — the field is immutable after creation"
  artifacts:
    - path: "anotame-api/backend/sales-service/src/main/resources/db/migration/V4__add_price_list_to_order.sql"
      provides: "Flyway migration adding price_list_id and price_list_name columns to tco_order"
      contains: "ALTER TABLE tco_order"
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java"
      provides: "DTO field priceListId for order creation"
      exports: ["priceListId"]
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/OrderResponse.java"
      provides: "Response fields priceListId and priceListName"
      exports: ["priceListId", "priceListName"]
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java"
      provides: "Domain model field priceListId and priceListName"
      contains: "priceListId"
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java"
      provides: "JPA entity columns price_list_id and price_list_name"
      contains: "price_list_id"
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderPersistenceAdapter.java"
      provides: "Manual entity↔domain mapping for priceListId/priceListName"
      contains: "priceListId"
    - path: "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java"
      provides: "Service layer passes priceListId through createOrder() and mapToResponse()"
      contains: "priceListId"
  key_links:
    - from: "CreateOrderRequest.priceListId"
      to: "Order.priceListId"
      via: "SalesService.createOrder() assignment"
      pattern: "order\\.setPriceListId"
    - from: "Order.priceListId"
      to: "OrderEntity.priceListId"
      via: "OrderPersistenceAdapter.save() entity mapping"
      pattern: "entity\\.setPriceListId"
    - from: "OrderEntity.priceListId"
      to: "Order.priceListId"
      via: "OrderPersistenceAdapter.toDomain() mapping"
      pattern: "o\\.setPriceListId"
    - from: "Order.priceListId"
      to: "OrderResponse.priceListId"
      via: "SalesService.mapToResponse() builder"
      pattern: "\\.priceListId\\(order\\.getPriceListId\\(\\)\\)"
---

<objective>
Add `priceListId` and `priceListName` fields to the entire backend stack for the sales-service: Flyway migration, DTO layer, domain model, JPA entity, persistence adapter, and service layer.

Purpose: Provide the data foundation for the frontend to store and retrieve which price list was selected at order creation time. The fields are nullable — no validation, no cross-service checks.
Output: Backend accepts, persists, and returns `priceListId`/`priceListName` in the order creation/retrieval lifecycle.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-price-list-selection-in-order-wizard/16-CONTEXT.md
@.planning/phases/16-price-list-selection-in-order-wizard/16-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flyway migration V4 + backend DTO, domain, entity fields</name>
  <files>
    anotame-api/backend/sales-service/src/main/resources/db/migration/V4__add_price_list_to_order.sql
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/OrderResponse.java
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java
  </files>
  <read_first>
    - anotame-api/backend/sales-service/src/main/resources/db/migration/V3__order_lifecycle_improvements.sql (verify V3 is latest, V4 name will not conflict)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java (current DTO shape — nullable `notes` is the pattern to follow for `priceListId`)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/OrderResponse.java (current builder DTO shape — add two nullable fields)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java (current `@Data` domain model — add two fields)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java (current `@Getter @Setter` entity — must NOT become `@Data`)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/UpdateOrderRequest.java (must NOT be modified — verify `priceListId` is absent and stays absent)
  </read_first>
  <behavior>
    - Flyway SQL: CREATE file V4__add_price_list_to_order.sql — two nullable columns added to tco_order
    - CreateOrderRequest: field `priceListId` nullable UUID with no validation annotation → accepts null silently
    - CreateOrderRequest: field `priceListName` nullable String with no validation annotation → accepts null silently (denormalized for read-back)
    - OrderResponse: fields `priceListId` UUID and `priceListName` String visible in the builder
    - Order domain model: fields `priceListId` UUID and `priceListName` String
    - OrderEntity: fields `priceListId` UUID with `@Column(name = "price_list_id")` and `priceListName` String with `@Column(name = "price_list_name", length = 255)` — no `@NotNull`, entity stays `@Getter @Setter` only
  </behavior>
  <action>
1. Create `V4__add_price_list_to_order.sql`:

```sql
-- V4__add_price_list_to_order.sql
-- Additive migration: adds price list tracking to orders.
-- Both columns nullable — zero risk to existing rows.
ALTER TABLE tco_order
    ADD COLUMN IF NOT EXISTS price_list_id   UUID,
    ADD COLUMN IF NOT EXISTS price_list_name VARCHAR(255);
```

2. Edit `CreateOrderRequest.java` — add after the `notes` field:

```java
private java.util.UUID priceListId;
private String priceListName;
```

No `@NotNull`, no `@Valid`, no validation annotation. `@Data` is already present — Lombok generates the getters/setters.

3. Edit `OrderResponse.java` — add after the `deliveredAt` field in the class body:

```java
private java.util.UUID priceListId;
private String priceListName;
```

`@Data @Builder` is already present — the builder will pick up both fields automatically.

4. Edit `Order.java` (domain model) — add after `deliveredAt`:

```java
private java.util.UUID priceListId;
private String priceListName;
```

`@Data` is already present.

5. Edit `OrderEntity.java` — add after the `deliveredAt` column field:

```java
@Column(name = "price_list_id")
private java.util.UUID priceListId;

@Column(name = "price_list_name", length = 255)
private String priceListName;
```

**CRITICAL:** Do NOT add `@Data` to `OrderEntity`. It already has `@Getter @Setter` only — keep it that way to avoid Hibernate hashCode/equals recursion. The new fields only need `@Getter @Setter`, which is already class-level.

6. Verify `UpdateOrderRequest.java` does NOT contain `priceListId` — do not modify it.
  </action>
  <verify>
    <automated>
      grep -n "priceListId" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java" && grep -n "priceListId" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/OrderResponse.java" && grep -n "priceListId" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java" && grep -n "price_list_id" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java" && cat "anotame-api/backend/sales-service/src/main/resources/db/migration/V4__add_price_list_to_order.sql" && grep -c "priceListId" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/UpdateOrderRequest.java" | grep -q "^0$" && echo "D-12 OK: UpdateOrderRequest has no priceListId"
    </automated>
  </verify>
  <acceptance_criteria>
    - `CreateOrderRequest.java` contains `private java.util.UUID priceListId;` with no validation annotation on that field
    - `CreateOrderRequest.java` contains `private String priceListName;` with no validation annotation on that field
    - `OrderResponse.java` contains `private java.util.UUID priceListId;`
    - `OrderResponse.java` contains `private String priceListName;`
    - `Order.java` contains `private java.util.UUID priceListId;`
    - `Order.java` contains `private String priceListName;`
    - `OrderEntity.java` contains `@Column(name = "price_list_id")` immediately before `private java.util.UUID priceListId;`
    - `OrderEntity.java` contains `@Column(name = "price_list_name", length = 255)` immediately before `private String priceListName;`
    - `OrderEntity.java` still has `@Getter` and `@Setter` and does NOT have `@Data`
    - `V4__add_price_list_to_order.sql` exists and contains `ADD COLUMN IF NOT EXISTS price_list_id   UUID`
    - `V4__add_price_list_to_order.sql` contains `ADD COLUMN IF NOT EXISTS price_list_name VARCHAR(255)`
    - `UpdateOrderRequest.java` does NOT contain the string `priceListId` (grep returns 0 matches)
  </acceptance_criteria>
  <done>All five backend model files modified with priceListId/priceListName fields, Flyway V4 migration created, UpdateOrderRequest untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence adapter mapping + service layer pass-through</name>
  <files>
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderPersistenceAdapter.java
    anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java
  </files>
  <read_first>
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderPersistenceAdapter.java (read the full file — locate `save()` method around line 34–108 and `toDomain()` method around line 173–237; add priceListId mapping in both)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java (read the full file — locate `createOrder()` around line 56–128 and `mapToResponse()` around line 137–189; add priceListId pass-through in both)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/CreateOrderRequest.java (after Task 1 — confirm `getPriceListId()` and `getPriceListName()` are generated by Lombok @Data)
    - anotame-api/backend/sales-service/src/main/java/com/anotame/sales/domain/model/Order.java (after Task 1 — confirm `setPriceListId()` and `getPriceListId()` generated by @Data)
  </read_first>
  <behavior>
    - OrderPersistenceAdapter.save(): copies priceListId and priceListName from Order domain model to OrderEntity
    - OrderPersistenceAdapter.toDomain(): copies priceListId and priceListName from OrderEntity back to Order domain model
    - SalesService.createOrder(): sets order.priceListId and order.priceListName from request fields
    - SalesService.mapToResponse(): includes priceListId and priceListName in the OrderResponse builder call
  </behavior>
  <action>
In `OrderPersistenceAdapter.java`:

1. In the `save()` method — after the line `entity.setPickupCode(...)` block (around line 57–59), add before the `// Map items` comment:

```java
entity.setPriceListId(order.getPriceListId());
entity.setPriceListName(order.getPriceListName());
```

2. In the `toDomain()` method — after the line `o.setDeliveredAt(entity.getDeliveredAt());` (around line 191), add:

```java
o.setPriceListId(entity.getPriceListId());
o.setPriceListName(entity.getPriceListName());
```

In `SalesService.java`:

3. In the `createOrder()` method — after the line `order.setPaymentMethod(request.getPaymentMethod());` (around line 76), add:

```java
order.setPriceListId(request.getPriceListId());
order.setPriceListName(request.getPriceListName());
```

4. In the `mapToResponse()` method — in the `OrderResponse.builder()` chain near the end (around line 173–188), add before `.build()`:

```java
.priceListId(order.getPriceListId())
.priceListName(order.getPriceListName())
```

The builder pattern in `mapToResponse` uses chained `.field(value)` calls — append these two before the final `.build()`.

5. Verify `UpdateOrderRequest` is never touched by these changes. The `updateOrder()` method in `SalesService` (if present) must NOT assign `priceListId`.
  </action>
  <verify>
    <automated>
      grep -n "setPriceListId\|setPriceListName" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderPersistenceAdapter.java" && grep -n "setPriceListId\|setPriceListName" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java" && grep -n "priceListId(order" "anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java"
    </automated>
  </verify>
  <acceptance_criteria>
    - `OrderPersistenceAdapter.java` contains `entity.setPriceListId(order.getPriceListId());` inside the `save()` method
    - `OrderPersistenceAdapter.java` contains `entity.setPriceListName(order.getPriceListName());` inside the `save()` method
    - `OrderPersistenceAdapter.java` contains `o.setPriceListId(entity.getPriceListId());` inside the `toDomain()` method
    - `OrderPersistenceAdapter.java` contains `o.setPriceListName(entity.getPriceListName());` inside the `toDomain()` method
    - `SalesService.java` contains `order.setPriceListId(request.getPriceListId());` inside `createOrder()`
    - `SalesService.java` contains `order.setPriceListName(request.getPriceListName());` inside `createOrder()`
    - `SalesService.java` contains `.priceListId(order.getPriceListId())` inside the `mapToResponse()` builder chain
    - `SalesService.java` contains `.priceListName(order.getPriceListName())` inside the `mapToResponse()` builder chain
    - `cd anotame-api/backend/sales-service && ./mvnw compile -q` exits 0 (no compile errors)
  </acceptance_criteria>
  <done>Backend fully wired: priceListId and priceListName flow from HTTP request → domain model → JPA entity → database, and back from database → domain model → HTTP response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → OrdersResource | All order creation requests cross this boundary; `priceListId` is a user-supplied UUID |
| sales-service → database | priceListId written to tco_order via JPA/Flyway column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | `CreateOrderRequest.priceListId` | accept | Field is nullable UUID with no validation — stores value as-is per D-09. No cross-service validation is intentional for this phase. Rogue UUIDs are stored but have no downstream effect since catalog-service is not queried at creation time. Low risk. |
| T-16-01-02 | Information Disclosure | `OrderResponse.priceListId` | accept | priceListId is a catalog-service entity identifier — not PII, not a secret. Returning it in the response is required for D-08. Any authenticated user who can read an order can already read all order details. |
| T-16-01-03 | Elevation of Privilege | `UpdateOrderRequest` mutation | mitigate | D-12: `UpdateOrderRequest.java` must NOT contain `priceListId`. The edit endpoint must not allow changing the price list after creation. Mitigation: `UpdateOrderRequest.java` is not modified in this plan; grep in acceptance criteria confirms 0 occurrences. |
| T-16-01-04 | Tampering | Flyway migration V4 | accept | Migration is additive (ADD COLUMN IF NOT EXISTS) and nullable. No existing row data is altered. IF NOT EXISTS guards against double-run. Standard Flyway checksum protection prevents replay. |
</threat_model>

<verification>
After completing both tasks:

1. `cd anotame-api/backend/sales-service && ./mvnw compile -q` — exits 0
2. `grep -c "priceListId" anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/dto/UpdateOrderRequest.java` — outputs `0`
3. `cat anotame-api/backend/sales-service/src/main/resources/db/migration/V4__add_price_list_to_order.sql` — shows both ADD COLUMN statements
4. `grep -c "@Data" anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/entity/OrderEntity.java` — outputs `0` (entity must remain @Getter @Setter only)
</verification>

<success_criteria>
- `mvnw compile -q` exits 0 with no errors
- All 7 backend files modified/created as specified
- `priceListId` field chain is complete: CreateOrderRequest → Order → OrderEntity → DB column → Order → OrderResponse
- `UpdateOrderRequest` contains 0 occurrences of `priceListId`
- `OrderEntity` still uses `@Getter @Setter` (not `@Data`)
- `V4__add_price_list_to_order.sql` creates both nullable columns with IF NOT EXISTS guard
</success_criteria>

<output>
After completion, create `.planning/phases/16-price-list-selection-in-order-wizard/16-01-SUMMARY.md`
</output>
