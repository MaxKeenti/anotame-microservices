---
phase: 11-datatablewrapper-filter-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-web/src/lib/components/ui/DataTableWrapper.svelte
  - anotame-web/src/routes/(app)/dashboard/customers/+page.svelte
  - anotame-web/src/routes/(app)/dashboard/orders/+page.svelte
autonomous: true
requirements:
  - TABLE-01
  - TABLE-02
  - TABLE-03
must_haves:
  truths:
    - "Customers page shows exactly one search input (its own form, not DataTableWrapper's)"
    - "Orders page shows exactly one filter UI (its own multi-filter form, not DataTableWrapper's)"
    - "All other data table pages (Garments, Services, Price Lists, Users, Schedule) show DataTableWrapper's built-in filter unchanged"
    - "All data table pages display a visible divider between filter area and table content"
    - "Build passes with `bun run build` without TypeScript errors"
  artifacts:
    - path: "anotame-web/src/lib/components/ui/DataTableWrapper.svelte"
      provides: "Configurable filter visibility via showFilter prop + horizontal divider"
      changes: "Add showFilter prop to Props type, add default showFilter=true to destructuring, wrap filter input in {#if showFilter} block, add divider div after filter section"
    - path: "anotame-web/src/routes/(app)/dashboard/customers/+page.svelte"
      provides: "Customers page using own search form, hiding wrapper's filter"
      changes: "Add showFilter={false} prop to DataTableWrapper component"
    - path: "anotame-web/src/routes/(app)/dashboard/orders/+page.svelte"
      provides: "Orders page using own multi-filter form, hiding wrapper's filter"
      changes: "Add showFilter={false} prop to DataTableWrapper component"
  key_links:
    - from: "DataTableWrapper.svelte (showFilter prop)"
      to: "Customers and Orders pages"
      via: "prop binding"
      pattern: "showFilter={false}"
    - from: "DataTableWrapper divider styling"
      to: "layout.css --border token"
      via: "CSS class binding"
      pattern: "border-border"
    - from: "Customers page search form"
      to: "DataTableWrapper data prop"
      via: "pre-filtered customers array"
      pattern: "data={customers}"
    - from: "Orders page $derived filter logic"
      to: "DataTableWrapper data prop"
      via: "pre-filtered orders array"
      pattern: "data={filteredOrders}"
---

<objective>
Eliminate duplicate filter UIs across data table pages by making DataTableWrapper's built-in search filter conditionally visible. Pages with custom server-side search (Customers, Orders) will hide the wrapper's filter and use their own. Pages without custom filters will keep DataTableWrapper's built-in client-side search. Add a visual divider between filter and table areas to clarify the boundary.

Purpose: Clear filter hierarchy, eliminate user confusion from duplicate search inputs, maintain backward compatibility on 5+ pages that don't need custom filters.

Output: 3 modified files (1 component + 2 pages), all 7 data table pages render without duplicate filters, build passes.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.planning/phases/11-datatablewrapper-filter-consolidation/11-CONTEXT.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.planning/phases/11-datatablewrapper-filter-consolidation/11-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Phase 10 Decisions (foundation for this phase)

From 10-CONTEXT.md: "Kept all 14 custom semantic tokens unchanged — already WCAG-compliant" and "Accepted all 17 preset components". This phase builds on the design token system from Phase 10, specifically the `--border` CSS variable now available in layout.css.

## Existing DataTableWrapper Usage

All 7 data table pages import and use DataTableWrapper:
- Customers: `/dashboard/customers/+page.svelte` — has custom `handleSearch()` form (lines 90-99)
- Orders: `/dashboard/orders/+page.svelte` — has custom $derived filter logic for search + garment + date filtering
- Garments: `/dashboard/catalog/garments/+page.svelte` — no custom filter, uses wrapper's built-in
- Services: `/dashboard/catalog/services/+page.svelte` — no custom filter, uses wrapper's built-in
- Price Lists: `/dashboard/catalog/price-lists/+page.svelte` — no custom filter, uses wrapper's built-in
- Users: `/dashboard/users/+page.svelte` — no custom filter, uses wrapper's built-in
- Schedule: `/dashboard/schedule/+page.svelte` — no custom filter, uses wrapper's built-in

Customers page search form structure (to understand pattern):
```svelte
<form onsubmit={handleSearch} class="flex-1 flex gap-2">
  <Input placeholder="Buscar clientes..." bind:value={searchQuery} class="max-w-md h-12" />
  <Button type="submit" variant="secondary" class="h-12 px-6">Buscar</Button>
</form>
```

Orders page filter structure (to understand pattern):
```svelte
<div class="grid grid-cols-1 md:grid-cols-4 gap-4 p-5 bg-card border border-border rounded-xl shadow-sm">
  <!-- Three separate filter inputs: search, garment select, date picker -->
</div>
<DataTableWrapper columns={activeColumns} data={filteredOrders} />
```

Design token availability (from layout.css):
- Light mode: `--border: oklch(0.922 0.005 34.3);` (subtle light gray)
- Dark mode: `--border: oklch(1 0 0 / 10%);` (subtle dark overlay)
- Tailwind class available: `border-border` applies the token
</context>

<tasks>

<task type="auto" wave="1">
  <name>Task 1: Add showFilter prop and divider to DataTableWrapper component</name>
  <files>anotame-web/src/lib/components/ui/DataTableWrapper.svelte</files>
  <read_first>
    - anotame-web/src/lib/components/ui/DataTableWrapper.svelte (current component to modify)
    - anotame-web/src/routes/layout.css (to verify --border token availability)
  </read_first>
  <action>
Modify DataTableWrapper.svelte:

1. **Add `showFilter` prop to the Props type (after line 25):**
   Insert after `filterPlaceholder?: string;`:
   ```typescript
   showFilter?: boolean;
   ```

2. **Add `showFilter` default to the destructuring (after line 36, before actionCell):**
   Insert after `filterPlaceholder = 'Buscar...',`:
   ```typescript
   showFilter = true,
   ```

3. **Wrap the search input in a conditional block (lines 109-117 in current code):**
   Replace the current `<div>` containing the search input with:
   ```svelte
   {#if showFilter}
     <div>
       <label for="dt-filter" class="sr-only text-sm font-medium">Buscar</label>
       <Input
         id="dt-filter"
         placeholder={filterPlaceholder}
         bind:value={globalFilter}
         class="h-12 touch-manipulation"
       />
     </div>
   {/if}
   ```

4. **Add a divider element after the filter section (after the {/if} block, before the table div):**
   Insert a new line:
   ```svelte
   <div class="border-t border-border"></div>
   ```

The divider should always be present (regardless of showFilter value) using the --border CSS token from Phase 10 design system. This provides visual separation between filter area and table content.

**Why these changes:**
- Default `showFilter = true` ensures backward compatibility — all 7 existing pages work without modification
- Conditional rendering via {#if showFilter} only hides the input element, not the divider
- Divider uses `border-border` Tailwind class which maps to --border CSS variable (supports both light/dark themes)
- No other component logic or state changes needed
  </action>
  <verify>
    <automated>
      - File contains `showFilter?: boolean;` in Props type: `grep -n "showFilter.*boolean" anotame-web/src/lib/components/ui/DataTableWrapper.svelte`
      - File contains `showFilter = true,` in destructuring: `grep -n "showFilter = true" anotame-web/src/lib/components/ui/DataTableWrapper.svelte`
      - File contains conditional filter block: `grep -n "{#if showFilter}" anotame-web/src/lib/components/ui/DataTableWrapper.svelte`
      - File contains divider element: `grep -n 'class="border-t border-border"' anotame-web/src/lib/components/ui/DataTableWrapper.svelte`
      - Build passes: `cd anotame-web && bun run build 2>&1 | grep -q "Built successfully"` (or similar success indicator)
    </automated>
  </verify>
  <acceptance_criteria>
    - DataTableWrapper.svelte has `showFilter?: boolean;` in Props type definition
    - DataTableWrapper.svelte has `showFilter = true,` with default value in destructuring
    - Filter input section is wrapped in `{#if showFilter}...{/if}` block
    - Divider element `<div class="border-t border-border"></div>` exists after filter block
    - Component compiles without TypeScript errors
    - Backward compatibility maintained: prop is optional and defaults to true
  </acceptance_criteria>
  <done>
DataTableWrapper component accepts optional `showFilter` prop (defaults to true). When showFilter={true}, built-in filter input is rendered. When showFilter={false}, filter input is hidden. Horizontal divider using --border token is always present. Component compiles without errors and is backward compatible — existing pages not passing showFilter prop still render the filter.
  </done>
</task>

<task type="auto" wave="2">
  <name>Task 2: Update Customers page to hide DataTableWrapper's filter and use its own</name>
  <files>anotame-web/src/routes/(app)/dashboard/customers/+page.svelte</files>
  <read_first>
    - anotame-web/src/routes/(app)/dashboard/customers/+page.svelte (to locate DataTableWrapper usage)
    - anotame-web/src/lib/components/ui/DataTableWrapper.svelte (to confirm showFilter prop signature)
  </read_first>
  <action>
Modify customers/+page.svelte:

**Locate the DataTableWrapper component usage** (approximately line 95 based on earlier code snippet) and add the `showFilter={false}` prop:

```svelte
<DataTableWrapper
  columns={columns}
  data={customers}
  loading={loading}
  emptyMessage="No se encontraron clientes."
  filterPlaceholder="Filtrar clientes..."
  showFilter={false}
  <!-- ^ NEW LINE: Hide wrapper's built-in filter, use custom form instead -->
>
  {#snippet actionCell(row)}
    <!-- ... existing snippet code ... -->
  {/snippet}
</DataTableWrapper>
```

**Why this works:**
- Customers page already has a custom search form with handleSearch() function (lines ~47-50 in full file)
- The form calls `fetchCustomers(searchQuery)` which makes API call to `/api/customers/search?query=`
- Data is pre-filtered at API level before reaching DataTableWrapper
- Passing `showFilter={false}` hides the wrapper's built-in filter input entirely
- Result: Single search UI (the page's form), no duplication
- Divider from wrapper still appears, separating the custom filter form from the table

**No other changes needed** — the custom search form and handleSearch handler remain unchanged. This is a prop-only modification.
  </action>
  <verify>
    <automated>
      - File contains `showFilter={false}` in DataTableWrapper component: `grep -n 'showFilter={false}' anotame-web/src/routes/\(app\)/dashboard/customers/+page.svelte`
      - File contains the custom search form structure unchanged: `grep -n 'handleSearch' anotame-web/src/routes/\(app\)/dashboard/customers/+page.svelte`
      - Build passes: `cd anotame-web && bun run build 2>&1 | grep -q "Built successfully"` (or similar success indicator)
    </automated>
  </verify>
  <acceptance_criteria>
    - Customers page DataTableWrapper component has `showFilter={false}` prop
    - Custom search form (with Input and handleSearch) remains present and unchanged
    - No duplicate filter inputs on the page (wrapper's filter is hidden, only custom form visible)
    - Page compiles without TypeScript errors
    - fetchCustomers() API call still filters data before passing to wrapper
  </acceptance_criteria>
  <done>
Customers page passes `showFilter={false}` to DataTableWrapper. Page's custom search form remains the only visible filter input. API-filtered data is passed to wrapper. Divider appears below the custom filter form, separating it from the table. No duplicate filters.
  </done>
</task>

<task type="auto" wave="2">
  <name>Task 3: Update Orders page to hide DataTableWrapper's filter and use its own multi-filter</name>
  <files>anotame-web/src/routes/(app)/dashboard/orders/+page.svelte</files>
  <read_first>
    - anotame-web/src/routes/(app)/dashboard/orders/+page.svelte (to locate DataTableWrapper usage)
    - anotame-web/src/lib/components/ui/DataTableWrapper.svelte (to confirm showFilter prop signature)
  </read_first>
  <action>
Modify orders/+page.svelte:

**Locate the DataTableWrapper component usage** (approximately line 140+ based on earlier code snippet) and add the `showFilter={false}` prop:

```svelte
<DataTableWrapper
  columns={activeColumns}
  data={filteredOrders}
  loading={loading}
  emptyMessage="No se encontraron pedidos."
  filterPlaceholder="Buscar pedidos..."
  showFilter={false}
  <!-- ^ NEW LINE: Hide wrapper's built-in filter, use custom multi-filter form instead -->
>
  {#snippet actionCell(row)}
    <!-- ... existing snippet code ... -->
  {/snippet}
</DataTableWrapper>
```

**Why this works:**
- Orders page has a custom multi-filter form with three dimensions: search (Input), garment filter (AdaptiveSelect), and delivery date filter (AdaptiveDatePicker)
- Page uses `$derived.by()` to compute `filteredOrders` based on searchQuery, garmentFilter, and dateFilter state
- Pre-filtered data (filteredOrders) is passed to DataTableWrapper
- Passing `showFilter={false}` hides the wrapper's built-in filter input entirely
- Result: Single comprehensive filter UI (the page's multi-filter form), no duplication
- Divider from wrapper still appears, separating the custom filter form from the table

**No other changes needed** — the custom filter form and $derived filter logic remain unchanged. This is a prop-only modification.
  </action>
  <verify>
    <automated>
      - File contains `showFilter={false}` in DataTableWrapper component: `grep -n 'showFilter={false}' anotame-web/src/routes/\(app\)/dashboard/orders/+page.svelte`
      - File contains the custom filter form with three inputs unchanged: `grep -n 'searchQuery\|garmentFilter\|dateFilter' anotame-web/src/routes/\(app\)/dashboard/orders/+page.svelte`
      - File contains the $derived filter logic unchanged: `grep -n '$derived.by' anotame-web/src/routes/\(app\)/dashboard/orders/+page.svelte`
      - Build passes: `cd anotame-web && bun run build 2>&1 | grep -q "Built successfully"` (or similar success indicator)
    </automated>
  </verify>
  <acceptance_criteria>
    - Orders page DataTableWrapper component has `showFilter={false}` prop
    - Custom multi-filter form (with search Input, garment Select, date picker) remains present and unchanged
    - No duplicate filter inputs on the page (wrapper's filter is hidden, only custom form visible)
    - Page compiles without TypeScript errors
    - $derived.by() filter logic still computes filteredOrders from three filter dimensions
    - filteredOrders pre-filtered data is passed to wrapper before filtering occurs
  </acceptance_criteria>
  <done>
Orders page passes `showFilter={false}` to DataTableWrapper. Page's custom multi-filter form (search + garment + date) remains the only visible filter UI. Pre-filtered data (from $derived) is passed to wrapper. Divider appears below the custom filter form, separating it from the table. No duplicate filters.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. **Build verification:** Run `bun run build` in anotame-web directory. Must exit with 0 and produce no TypeScript errors related to DataTableWrapper or the two pages.

2. **Visual verification (manual, in dev environment):**
   - Navigate to `/dashboard/customers` — should show ONE search form (custom), NOT two
   - Navigate to `/dashboard/orders` — should show ONE multi-filter form (search + garment + date), NOT two
   - Navigate to `/dashboard/catalog/garments` — should show wrapper's built-in search (no custom filter)
   - All pages should display a subtle horizontal line (divider) between filter area and table
   - Divider should be visible in both light and dark modes

3. **Backward compatibility check:** All 7 pages (Customers, Orders, Garments, Services, Price Lists, Users, Schedule) should render without errors in dev environment.

4. **Requirement mapping:**
   - TABLE-01 (no duplicate filters): Customers and Orders pages each show exactly one filter UI
   - TABLE-02 (DataTableWrapper accepts filter config): Component accepts showFilter prop and respects it
   - TABLE-03 (visual separation): Divider is present on all pages, separates filter from table
</verification>

<success_criteria>
1. `bun run build` passes with zero TypeScript errors
2. Customers page renders with only one search input (custom form), wrapper's filter hidden
3. Orders page renders with only one filter UI (custom multi-filter), wrapper's filter hidden
4. Garments, Services, Price Lists, Users, and Schedule pages render with wrapper's built-in filter visible (backward compatible)
5. All 7 pages display a visible divider between filter area and table content
6. Divider is styled using --border CSS variable (visible in both light and dark themes)
7. All three requirements (TABLE-01, TABLE-02, TABLE-03) are demonstrably satisfied
</success_criteria>

<output>
After plan completion, create `.planning/phases/11-datatablewrapper-filter-consolidation/11-01-SUMMARY.md` documenting:
- What was built (showFilter prop, conditional rendering, divider, page updates)
- Files modified (3 files)
- Verification results (build pass, visual checks on all 7 pages)
- Requirements satisfied (TABLE-01, TABLE-02, TABLE-03)
</output>
