---
phase: 21-local-dev-docker-compose
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DEV-03]

must_haves:
  truths:
    - "README.md describes the current 4-service Quarkus architecture (not Spring Boot)"
    - "README.md contains a Local Development section with the exact docker compose up -d command"
    - "README.md explains that Flyway creates the schema automatically on first quarkus:dev start — no manual SQL"
    - "README.md warns that catalog-db uses port 5432 (PostgreSQL default) and a local PostgreSQL server must be stopped"
    - "README.md lists the one-time teardown command for the old postgres_data volume"
    - "README.md does NOT describe docker-compose up --build as the way to run the full stack"
  artifacts:
    - path: "README.md"
      provides: "Accurate local dev bring-up instructions for Phase 21 workflow"
      contains: "docker compose up -d"
    - path: "README.md"
      provides: "Accurate local dev bring-up instructions for Phase 21 workflow"
      contains: "quarkus:dev"
  key_links:
    - from: "README.md local dev steps"
      to: "docker-compose.yml (Plan 01)"
      via: "docker compose up -d starts the 4 DB containers Plan 01 defines"
    - from: "README.md Flyway note"
      to: "quarkus.flyway.migrate-at-start=true in application.properties"
      via: "Flyway runs automatically on quarkus:dev start; no manual psql step needed"
---

<objective>
Rewrite README.md to accurately document the local development workflow after Phase 21. The current README is outdated — it references Spring Boot (the backend is Quarkus), describes a single shared PostgreSQL container on port 5433, and instructs developers to run `docker-compose up --build` to start the full stack. After Phase 21, the correct workflow is: `docker compose up -d` (starts 4 DB containers), then `./mvnw quarkus:dev` per service.

Purpose: DEV-03 requires the local dev bring-up to be documented so a new developer can get started without hunting through compose files. The README is the first document they read.

Output: A rewritten README.md with accurate architecture overview, correct prerequisites, and a step-by-step local dev section that matches the actual Phase 21 compose file.
</objective>

<execution_context>
@/Users/moonstone/Source/Personal/anotame-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moonstone/Source/Personal/anotame-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moonstone/Source/Personal/anotame-microservices/.planning/ROADMAP.md
@/Users/moonstone/Source/Personal/anotame-microservices/.planning/REQUIREMENTS.md
@/Users/moonstone/Source/Personal/anotame-microservices/.planning/phases/21-local-dev-docker-compose/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with accurate local dev workflow</name>
  <files>README.md</files>
  <read_first>
    - README.md (read the full current file before rewriting — identify all sections, some may be worth preserving or updating rather than discarding)
    - docker-compose.yml (read after Plan 01 has run OR read current to see what will be there — confirm port numbers 5431–5434 and container names)
    - .planning/phases/21-local-dev-docker-compose/21-RESEARCH.md (section: "Port Mapping Table" and "Flyway Auto-Schema Pattern")
  </read_first>
  <action>
Replace the entire contents of README.md. The new file must contain the following sections. Write in plain markdown — no emojis.

---

# Anotame Microservices

A microservices-based backend for the Anotame platform, built with Java Quarkus and Hexagonal Architecture.

## Architecture

The system consists of 4 backend services and 1 SvelteKit frontend:

- **identity-service** (port 8081): User registration, authentication, JWT issuance
- **catalog-service** (port 8082): Garment types, services, price lists
- **sales-service** (port 8083): Order creation and management
- **operations-service** (port 8084): Shifts, employee assignments, work orders
- **anotame-web**: SvelteKit frontend (port 3000 in dev)

All backend services follow Hexagonal Architecture (domain / application / infrastructure layers).

Each service owns its own PostgreSQL database — no shared database, no cross-service foreign keys.

## Prerequisites

- Docker Desktop (for local database containers)
- Java 21
- Maven (or use the `./mvnw` wrapper in each service directory)

## Local Development

### 1. Start the database containers

From the repo root:

```bash
docker compose up -d
```

This starts 4 independent PostgreSQL containers:

| Container | Host Port | DB Name |
|-----------|-----------|---------|
| identity-db | 5431 | identity |
| catalog-db | 5432 | catalog |
| sales-db | 5433 | sales |
| operations-db | 5434 | operations |

> **Note:** catalog-db binds to host port 5432, which is the PostgreSQL default. If you have a local PostgreSQL server installed, stop it before running `docker compose up` to avoid a port conflict.

PgAdmin is also available at http://localhost:5050 (login: admin@anotame.com / admin).

### 2. Start a service in dev mode

Navigate to the service directory and run:

```bash
cd anotame-api/backend/identity-service
./mvnw quarkus:dev
```

On first start, Flyway automatically creates the schema in the service's database container. No manual SQL execution is needed.

Repeat for each service you need running:

```bash
cd anotame-api/backend/catalog-service && ./mvnw quarkus:dev
cd anotame-api/backend/sales-service && ./mvnw quarkus:dev
cd anotame-api/backend/operations-service && ./mvnw quarkus:dev
```

### 3. Start the frontend

```bash
cd anotame-web
bun install
bun run dev
```

The frontend is available at http://localhost:3000.

### One-time migration from the old setup

If you previously ran `docker compose up` with the old shared `anotame-db` container, a `postgres_data` volume may still exist. Remove it with:

```bash
docker volume rm anotame-microservices_postgres_data
```

This volume is no longer used and safe to delete.

## Project Structure

```
anotame-api/backend/
  identity-service/     Java Quarkus — auth and users
  catalog-service/      Java Quarkus — garments, services, price lists
  sales-service/        Java Quarkus — orders
  operations-service/   Java Quarkus — shifts and work orders
anotame-web/            SvelteKit frontend
docker-compose.yml      Local dev database containers (4 PostgreSQL)
.env                    Local dev env vars (JWT keys, cookie config)
```

## Environment Variables

Copy `.env` to a local `.env` file and populate the JWT key pair. The JWT keys are already populated in `.env` for local development.

When running `quarkus:dev`, each service reads its datasource URL from its `%dev` profile in `application.properties` — no `QUARKUS_DATASOURCE_JDBC_URL` env var is needed locally.

In production (Railway), the following env vars must be set per service:
- `QUARKUS_DATASOURCE_JDBC_URL`
- `QUARKUS_DATASOURCE_USERNAME`
- `QUARKUS_DATASOURCE_PASSWORD`
- `PORT`
- `SMALLRYE_JWT_SIGN_KEY` (identity-service only)
- `MP_JWT_VERIFY_PUBLICKEY` (all services)

---

IMPORTANT: Do not add sections that are not listed above. Do not add emojis. Do not add a Known Issues section (the current one is stale). Do not reference Spring Boot anywhere — the backend is Quarkus. Do not reference test_integration.sh (this is not part of the current dev workflow).
  </action>
  <verify>
    <automated>grep "Spring Boot" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "docker-compose up --build" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "docker compose up -d" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "quarkus:dev" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "5432 (PostgreSQL default)\|port 5432" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "postgres_data" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep "Flyway" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
    <automated>grep -E "5431|5432|5433|5434" /Users/moonstone/Source/Personal/anotame-microservices/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Spring Boot" README.md` returns 0 matches (obsolete framework reference removed)
    - `grep "docker-compose up --build" README.md` returns 0 matches (old full-stack compose command removed)
    - `grep "docker compose up -d" README.md` returns at least 1 match (correct DB-only startup command present)
    - `grep "quarkus:dev" README.md` returns at least 2 matches (service start instructions present)
    - `grep "5431" README.md` returns at least 1 match (identity-db port documented)
    - `grep "5432" README.md` returns at least 1 match (catalog-db port documented)
    - `grep "5433" README.md` returns at least 1 match (sales-db port documented)
    - `grep "5434" README.md` returns at least 1 match (operations-db port documented)
    - `grep "Flyway" README.md` returns at least 1 match (auto-schema creation documented)
    - `grep "postgres_data" README.md` returns at least 1 match (one-time migration step documented)
    - `grep "5432.*default\|default.*5432" README.md` returns at least 1 match (port conflict warning present)
    - `grep "test_integration.sh" README.md` returns 0 matches (stale reference removed)
  </acceptance_criteria>
  <done>README.md accurately documents the Phase 21 local dev workflow: docker compose up -d starts 4 DB containers, quarkus:dev auto-runs Flyway on first start, port 5432 conflict warned, one-time postgres_data removal step included</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → developer | Documentation file — no executable code, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Information Disclosure | README.md credential examples | accept | README references dev-only credentials (admin/password) that are intentionally weak and localhost-bound; no prod credentials appear in documentation |
| T-21-07 | Information Disclosure | README.md env var list | accept | Listing env var names (not values) in a public README is standard practice; actual values live in .env which is gitignored for prod keys |
</threat_model>

<verification>
After task completes, perform a final sanity check:

```bash
# No obsolete references
grep -E "Spring Boot|docker-compose up --build|test_integration\.sh|anotame-db" README.md
# Expected: 0 lines

# Core workflow keywords present
grep -c "docker compose up\|quarkus:dev\|Flyway\|postgres_data" README.md
# Expected: 4 (one match each — confirms all key sections present)

# All 4 ports documented
grep -E "5431|5432|5433|5434" README.md | wc -l
# Expected: at least 4 lines (one per port)
```
</verification>

<success_criteria>
- README.md: `docker compose up -d` is the documented first step (not `docker-compose up --build`)
- README.md: quarkus:dev startup instructions present for each service
- README.md: Flyway auto-schema creation explicitly noted (no manual SQL step)
- README.md: port 5432 conflict warning present (stop local PostgreSQL before running compose)
- README.md: one-time `docker volume rm anotame-microservices_postgres_data` step documented
- README.md: no Spring Boot references, no test_integration.sh references, no anotame-db references
</success_criteria>

<output>
After completion, create `.planning/phases/21-local-dev-docker-compose/21-02-SUMMARY.md`
</output>
