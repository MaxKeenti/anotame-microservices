---
phase: quick
plan: 260405-l2d
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-web/src/routes/(app)/dashboard/customers/+page.svelte
autonomous: true
requirements: []
---

<objective>
Wrap customers filter in DataTableWrapper for UI consistency.

Purpose: Remove the duplicate search UI pattern and standardize on DataTableWrapper's built-in filter component. Aligns with phase [11-01] decision to conditionally hide wrapper filters; now making an exception to enable it for Customers due to standardization priority.

Output: Single page modification enabling client-side filtering via DataTableWrapper instead of custom server-side search form.
</objective>

<execution_context>
@anotame-web/src/lib/components/ui/DataTableWrapper.svelte
@.planning/STATE.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Enable DataTableWrapper filter and remove custom search form</name>
  <files>anotame-web/src/routes/(app)/dashboard/customers/+page.svelte</files>
  <action>
Make three focused changes:

1. Remove the separate search form (lines 90-99): Delete the entire `<div class="flex gap-2">` block containing the Input and search Button.

2. Change `showFilter={false}` to `showFilter={true}` on line 108 in the DataTableWrapper component props.

3. Simplify data loading:
   - Remove the `searchQuery` state variable (line 17) — no longer needed
   - Update `fetchCustomers()` function to always load all customers without query parameter: change `const q = query ? `?query=${query}` : '';` to just `const response = await apiService.request<any[]>(`${API_SALES}/api/customers/search`);`
   - Update `handleSearch()` function: delete this function entirely as it's no longer used
   - Update `handleDeleteClick()` and `handleFormSuccess()`: change `fetchCustomers(searchQuery)` calls to just `fetchCustomers()` (no arguments)

Note: This shifts filtering from server-side to client-side. The DataTableWrapper's built-in `globalFilter` will now handle filtering on the loaded data. All action handling (edit/delete) remains unchanged, the dialog pattern is preserved.
  </action>
  <verify>
    <automated>grep -n "showFilter={true}" anotame-web/src/routes/\(app\)/dashboard/customers/+page.svelte && grep -c "handleSearch" anotame-web/src/routes/\(app\)/dashboard/customers/+page.svelte | grep -q "^0$" && ! grep -n "searchQuery" anotame-web/src/routes/\(app\)/dashboard/customers/+page.svelte | head -1</automated>
  </verify>
  <done>
Customers page uses DataTableWrapper's built-in filter (showFilter=true), custom search form removed, data loads all customers on mount without query parameter, all action buttons functional
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Confirm `showFilter={true}` is set on DataTableWrapper
2. Confirm lines 90-99 (the separate search form) are deleted
3. Confirm `searchQuery` state variable is removed
4. Confirm `fetchCustomers()` calls no query parameter
5. Confirm `handleDeleteClick()` and `handleFormSuccess()` call `fetchCustomers()` without arguments
</verification>

<success_criteria>
- DataTableWrapper's built-in filter is visible and functional (client-side filtering on all loaded customers)
- No separate search form exists above the table
- All customers load on page mount
- Edit and delete actions work without errors
- Customer dialog remains functional
</success_criteria>

<output>
After completion, update STATE.md:
- Add to Quick Tasks Completed: `| 260405-l2d | Standardize customers filter in DataTableWrapper | 2026-04-05 | {commit_hash} | COMPLETED | .planning/quick/260405-l2d-wrap-customers-filter-in-datatablewrappe/ |`
- Update note in Decisions section: "[11-01]: Updated Customers page to use wrapper's filter (enabled standardization over custom server-side search)"
</output>
