---
quick_id: 260413-ann
description: Fix unsafe null type conversion warnings in SalesService and OrderAuditLogPersistenceAdapter related to Collector annotations
date: 2026-04-13
status: planning
---

# Quick Plan: Fix Collector Annotation Warnings

The previous fix using `Collectors.toList()` introduced new IDE warnings regarding unsafe null type conversions in the `Collector` generic types. This is likely due to strict nullability checks on record-derived streams. I will refactor the collection logic to use a more direct approach that avoids complex generic inference in collectors.

## Tasks

### 1. Update SalesService.java
- **Files**: `anotame-api/backend/sales-service/src/main/java/com/anotame/sales/application/service/SalesService.java`
- **Action**: Replace `stream().map(...).collect(...)` with a more direct approach using `forEach(list::add)` or `Collectors.toCollection(ArrayList::new)` with explicit typing.
- **Verify**: IDE warning should disappear.
- **Done**: [ ]

### 2. Update OrderAuditLogPersistenceAdapter.java
- **Files**: `anotame-api/backend/sales-service/src/main/java/com/anotame/sales/infrastructure/persistence/adapter/OrderAuditLogPersistenceAdapter.java`
- **Action**: Same as above.
- **Verify**: IDE warning should disappear.
- **Done**: [ ]
