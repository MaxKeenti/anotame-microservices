---
phase: fix-typescript-error
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["anotame-web/src/routes/(app)/+layout.svelte"]
autonomous: true
requirements: ["UI-TS-FIX"]
must_haves:
  truths:
    - "TypeScript recognizes 'establishmentTheme' on the 'data' prop in +layout.svelte"
    - "npm run check passes in anotame-web"
  artifacts:
    - path: "anotame-web/src/routes/(app)/+layout.svelte"
      provides: "Properly typed props for the layout"
  key_links:
    - from: "anotame-web/src/routes/(app)/+layout.svelte"
      to: "anotame-web/src/routes/(app)/$types"
      via: "import type { LayoutData }"
---

<objective>
Fix the TypeScript error "Property 'establishmentTheme' does not exist on type '{}'" in `+layout.svelte` by explicitly typing the `$props()` using `LayoutData`.

Purpose: Ensure type safety and fix build/IDE errors in the main application layout.
Output: Modified `+layout.svelte` with proper TypeScript annotations.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@anotame-web/src/routes/(app)/+layout.svelte
@anotame-web/src/routes/(app)/+layout.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Explicitly type $props in +layout.svelte</name>
  <files>anotame-web/src/routes/(app)/+layout.svelte</files>
  <action>
    - Import `LayoutData` from `./$types`.
    - Import `Snippet` from `svelte`.
    - Update the `$props()` declaration to explicitly type `data` as `LayoutData` and `children` as `Snippet`.
    - Ensure `data.establishmentTheme` access remains correct (no functional changes, just typing).
  </action>
  <verify>
    <automated>cd anotame-web && npm run check</automated>
  </verify>
  <done>
    - Property 'establishmentTheme' is recognized by TypeScript.
    - No regression in layout functionality.
  </done>
</task>

</tasks>

<verification>
Check that `npm run check` in `anotame-web` completes without errors related to `+layout.svelte`.
</verification>

<success_criteria>
The property `establishmentTheme` is accessible on the `data` object in `+layout.svelte` without TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-t8i-fix-typescript-error-property-establishm/01-fix-typescript-error-SUMMARY.md`
</output>
