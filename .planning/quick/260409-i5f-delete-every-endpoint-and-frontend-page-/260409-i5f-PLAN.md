---
phase: quick
plan: 260409-i5f
type: execute
description: Delete all signup endpoints and pages — system only allows pre-registered users
autonomous: true
requirements: []
---

<objective>
Remove every endpoint and frontend page that allows non-registered users to create a new account.

**Current State:** System has open signup via:
- Frontend: `/register` route with registration form
- Backend: `POST /auth/register` endpoint + `AuthService.register()` method
- Navigation: Signup button on home page (`+page.svelte`)

**Target State:** System allows login only. Registration removed entirely. No signup routes, no register endpoints, no signup links.

**Output:** Three focused deletions (frontend route, backend endpoint, navigation link)
</objective>

<execution_context>
Working directory: `/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices`

Codebase structure:
- **Frontend:** anotame-web/src/routes/register/+page.svelte (full page delete)
- **Backend:** 
  - anotame-api/.../AuthController.java (delete @POST /register method)
  - anotame-api/.../AuthService.java (delete register() and RegisterRequest handling)
- **Navigation:** anotame-web/src/routes/+page.svelte (delete signup button)
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Delete frontend register route and signup button from home page</name>
  <files>anotame-web/src/routes/register (entire directory), anotame-web/src/routes/+page.svelte</files>
  <action>
1. **Delete the register route directory:** Remove `anotame-web/src/routes/register/` folder entirely (contains +page.svelte registration form)
2. **Remove signup button from home page:** Edit `anotame-web/src/routes/+page.svelte` and delete the signup Button component. Search for `<Button href="/register"` and remove that entire button element (approximately lines with "Registrarse" or signup text).
3. **Verify no dangling imports:** After removals, check that +page.svelte has no imports specific to the register form (e.g., no RegisterRequest imports).
4. **Git:** Stage deletions with `git add -A && git status` to confirm register/ directory marked for deletion and +page.svelte changes staged.
  </action>
  <verify>
    <automated>
test -d anotame-web/src/routes/register && echo "FAIL: register directory still exists" || echo "PASS: register directory deleted"
grep -q "href=\"/register\"" anotame-web/src/routes/+page.svelte && echo "FAIL: signup link still present" || echo "PASS: signup link removed"
    </automated>
  </verify>
  <done>
- register/ directory deleted from anotame-web/src/routes/
- Signup button removed from +page.svelte (no href="/register" remaining)
- No broken imports in +page.svelte
- Changes staged in git
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete POST /auth/register endpoint from AuthController</name>
  <files>anotame-api/backend/identity-service/src/main/java/com/anotame/identity/infrastructure/web/controller/AuthController.java</files>
  <action>
1. **Open AuthController.java** and locate the `@POST @Path("/register")` method (around line 28-30 based on typical layout).
2. **Delete the entire register() method:**
   ```java
   @POST
   @Path("/register")
   public Response register(RegisterRequest request) {
       AuthResponse authResponse = service.register(request);
       return createCookieResponse(authResponse);
   }
   ```
3. **Remove RegisterRequest import** if it's no longer used elsewhere in the file. Check imports at the top of the file and remove `import com.anotame.identity.application.dto.RegisterRequest;` if present only for this method.
4. **Verify:** No compilation errors. Run `mvn clean compile` in identity-service directory to confirm no broken references.
  </action>
  <verify>
    <automated>
grep -q "@POST" anotame-api/backend/identity-service/src/main/java/com/anotame/identity/infrastructure/web/controller/AuthController.java | grep -q "register" && echo "FAIL: register method still in controller" || echo "PASS: register method removed"
    </automated>
  </verify>
  <done>
- @POST /register method deleted from AuthController
- RegisterRequest import removed (if unused)
- No compilation errors in identity-service
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete register() method from AuthService and clean RegisterRequest DTO</name>
  <files>anotame-api/backend/identity-service/src/main/java/com/anotame/identity/application/service/AuthService.java, anotame-api/backend/identity-service/src/main/java/com/anotame/identity/application/dto/RegisterRequest.java</files>
  <action>
1. **Open AuthService.java** and find the `public register(RegisterRequest request)` method.
2. **Delete the entire register() method** (and any private helper methods specific to registration logic).
3. **Remove RegisterRequest import** from AuthService if present.
4. **Delete RegisterRequest.java DTO file** entirely: `anotame-api/backend/identity-service/src/main/java/com/anotame/identity/application/dto/RegisterRequest.java`
   - This DTO is only used by the register endpoint, so safe to remove when endpoint is deleted.
5. **Verify no other files reference RegisterRequest:** Run `grep -r "RegisterRequest" anotame-api/` to confirm it's not imported/used elsewhere.
6. **Verify no other files call AuthService.register():** Run `grep -r "\.register(" anotame-api/` to confirm no remaining calls.
7. **Compile:** Run `mvn clean compile` in identity-service to ensure no broken references.
  </action>
  <verify>
    <automated>
grep -q "public.*register\|RegisterRequest" anotame-api/backend/identity-service/src/main/java/com/anotame/identity/application/service/AuthService.java && echo "FAIL: register method or RegisterRequest still in AuthService" || echo "PASS: register logic removed"
test -f anotame-api/backend/identity-service/src/main/java/com/anotame/identity/application/dto/RegisterRequest.java && echo "FAIL: RegisterRequest.java still exists" || echo "PASS: RegisterRequest.java deleted"
grep -r "RegisterRequest" anotame-api/ 2>/dev/null | wc -l | grep -q "^0$" && echo "PASS: no RegisterRequest references remain" || echo "WARN: RegisterRequest still referenced elsewhere"
    </automated>
  </verify>
  <done>
- register() method deleted from AuthService
- RegisterRequest DTO file deleted
- No remaining references to RegisterRequest in codebase
- No remaining calls to AuthService.register()
- Clean compilation in identity-service
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. **Frontend Verification:**
   - No `/register` route accessible (trying to navigate to `/register` should redirect or 404)
   - Home page loads without signup button
   - No errors in browser console related to missing register route

2. **Backend Verification:**
   - `curl -X POST http://localhost:8080/auth/register` returns 404 or method not allowed
   - Identity service compiles cleanly: `mvn clean package -DskipTests`
   - No error logs containing "register" in application startup

3. **Codebase Verification:**
   - `grep -r "register\|Register" anotame-web/src/routes/` shows only login and dashboard routes
   - `grep -r "RegisterRequest" anotame-api/` returns no results
   - Git status shows register/ directory deleted and modified +page.svelte, AuthController.java, AuthService.java
</verification>

<success_criteria>
✓ `/register` route completely removed from frontend
✓ Signup button deleted from home page
✓ `POST /auth/register` endpoint deleted from backend
✓ `AuthService.register()` method deleted
✓ `RegisterRequest` DTO removed entirely
✓ No compilation errors in identity-service
✓ No broken references to deleted signup functionality
✓ System ready for login-only access (pre-registered users only)
</success_criteria>
