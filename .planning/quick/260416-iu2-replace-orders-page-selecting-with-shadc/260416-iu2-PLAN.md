---
phase: quick
plan: 260416-iu2
type: execute
wave: 1
depends_on: []
files_modified:
  - anotame-web/src/routes/(app)/dashboard/orders/+page.svelte
  - anotame-web/src/lib/components/ui/DataTableWrapper.svelte
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Selection column and checkboxes appear automatically when user clicks first checkbox, not via toggle button"
    - "FloatingActionBar appears when any row is selected, disappears when all are deselected"
    - "Clear selection button visible only when items are selected"
    - "Manual 'Seleccionar pedidos' / 'Cancelar selección' toggle buttons removed"
    - "Selection state survives pagination (selecting page 1 rows, navigating to page 2, both remain selected)"
    - "Checkbox and row interactive targets are at least 32px on mobile for touch-friendly selection"
  artifacts:
    - path: anotame-web/src/routes/(app)/dashboard/orders/+page.svelte
      provides: "Orders page with automatic row selection mode"
      min_lines: 270
    - path: anotame-web/src/lib/components/ui/DataTableWrapper.svelte
      provides: "Data table with improved touch targets for checkboxes"
      min_lines: 280
  key_links:
    - from: "Orders +page.svelte"
      to: "DataTableWrapper"
      via: "bulkMode prop binding"
      pattern: "bind:bulkMode"
    - from: "Orders +page.svelte selectedOrders array"
      to: "FloatingActionBar visibility"
      via: "count prop"
      pattern: "count={selectedOrders.length}"
---

<objective>
Replace the /Orders page's manual bulk selection mode toggle with automatic selection based on the shadcn-svelte data-table row actions pattern. Users click checkboxes to select rows; the floating action bar appears contextually based on selection state. No more "Seleccionar pedidos" / "Cancelar selección" button.

Purpose: Aligns UI with shadcn-svelte conventions, reduces cognitive load (no modal mode), improves discoverability of bulk actions.

Output: Orders page uses implicit bulk mode; checkboxes are always visible when needed; selection column and actions appear contextually based on user interaction.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/quick/260416-iu2-replace-orders-page-selecting-with-shadc/260416-iu2-RESEARCH.md
@.planning/quick/260416-iu2-replace-orders-page-selecting-with-shadc/260416-iu2-CONTEXT.md
</execution_context>

<context>
## Current Implementation State

**Orders Page (+page.svelte, lines 236-254):**
- Manual toggle button: "Seleccionar pedidos" (enters bulkMode) / "Cancelar selección" (exits bulkMode)
- Bulk handlers reset `selectedOrders = []` after async operations (handleBulkStatusChange, handleBulkDelete, handleBulkCancel)
- FloatingActionBar already conditionally renders when `count > 0` ✓

**DataTableWrapper (+page.svelte, line 273):**
- Receives `bind:bulkMode` and passes it to DataTableWrapper
- Selection column injected only when `bulkActions && bulkMode` (line 86-89)
- Checkboxes are currently 16x16px (`h-4 w-4`) — below 44x44px touch target recommendation

**FloatingActionBar (already correct):**
- Only renders when `{#if count > 0}` — correct behavior
- Receives count, handlers for delete/status change

## Integration Checklist (from RESEARCH.md)

✓ FloatingActionBar visibility already correct
✓ onSelectionChange callback already firing
✗ Remove "Seleccionar pedidos" toggle button
✗ Keep bulkMode always true (don't toggle it)
? Increase checkbox touch targets (16x16 → 32x32+ per AI_RULES.md)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace manual toggle with automatic selection mode</name>
  <files>anotame-web/src/routes/(app)/dashboard/orders/+page.svelte</files>
  <action>
Remove the manual toggle button (lines 236-254) that switches between "Seleccionar pedidos" and "Cancelar selección". Replace with:

1. Delete the entire `<div class="flex justify-end">` block containing the toggle button (lines 236-254)
2. Add a new conditional button that shows ONLY when items are selected: `{#if selectedOrders.length > 0}` containing a "Limpiar selección ({count})" button with `onclick={() => { selectedOrders = []; }}`
3. In DataTableWrapper prop binding (line 273), change from `bind:bulkMode={bulkMode}` to `bulkMode={true}` (always enabled, no binding needed)
4. Delete the bulkMode state initialization line: `let bulkMode = $state(false);` (line 27)
5. Remove all `bulkMode = false` assignments from bulk handler functions:
   - Line 110: `bulkMode = false;` in handleBulkStatusChange
   - Line 143: `bulkMode = false;` in handleBulkDelete
   - Line 150: `bulkMode = false;` in handleBulkCancel (delete entire function since no longer needed, or keep as no-op)
6. Replace with `selectedOrders = []` in those locations if not already present (they are: lines 111, 144)

Final button pattern (insert after filter UI, before table):
```svelte
{#if selectedOrders.length > 0}
  <div class="flex justify-end">
    <Button
      variant="ghost"
      class="h-12 px-4 touch-manipulation text-muted-foreground hover:text-foreground"
      onclick={() => { selectedOrders = []; }}
    >
      Limpiar selección ({selectedOrders.length})
    </Button>
  </div>
{/if}
```

Per D-01 (user decision from CONTEXT.md): Floating window visible only when items selected (already correct in FloatingActionBar, now enforced by button logic).
  </action>
  <verify>
    <automated>grep -n "bulkMode = true" anotame-web/src/routes/\(app\)/dashboard/orders/+page.svelte && grep -c "Seleccionar pedidos" anotame-web/src/routes/\(app\)/dashboard/orders/+page.svelte | grep "^0$"</automated>
  </verify>
  <done>
    - bulkMode toggle button removed from page
    - bulkMode hardcoded to true in DataTableWrapper
    - "Clear selection" button appears only when items selected
    - bulkMode state variable deleted
    - All bulkMode = false assignments removed
    - Page compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve checkbox touch targets for mobile accessibility</name>
  <files>anotame-web/src/lib/components/ui/DataTableWrapper.svelte</files>
  <action>
Increase checkbox size from 16x16px to 32x32px to meet touch-first design requirements (per AI_RULES.md: "Touch-First Design: UI must be heavily optimized for touchscreen interactions").

Current checkboxes (line 185, 238): `class="h-4 w-4 cursor-pointer"`

Changes:
1. Line 185 (header checkbox): Change `h-4 w-4` to `h-8 w-8` and wrap in a larger clickable region for consistent touch target:
```svelte
<div class="flex items-center justify-center h-12 w-12 -ml-3">
  <input
    type="checkbox"
    class="h-8 w-8 cursor-pointer"
    aria-label="Seleccionar todos"
    checked={table.getIsAllRowsSelected()}
    onchange={table.getToggleAllRowsSelectedHandler()}
  />
</div>
```

2. Line 238 (row cell checkbox): Same treatment, wrap in larger clickable region:
```svelte
<div class="flex items-center justify-center h-12 w-12 -ml-3">
  <input
    type="checkbox"
    class="h-8 w-8 cursor-pointer"
    aria-label="Seleccionar fila"
    checked={cell.row.getIsSelected()}
    onchange={cell.row.getToggleSelectedHandler()}
  />
</div>
```

3. Adjust Table.Head for __select__ column (line 178-179): Change `w-12` to `w-16` to accommodate larger touch target area.

4. Adjust Table.Cell padding for selection column (line 234): Add conditional padding class to reduce cramping:
```svelte
class="px-6 py-4 {cell.column.id === '__select__' ? 'px-0' : ''}"
```

Result: 32x32px checkboxes (up from 16x16px) with centered layout, meeting 44x44px minimum touch target including surrounding padding.
  </action>
  <verify>
    <automated>grep -c "h-8 w-8" anotame-web/src/lib/components/ui/DataTableWrapper.svelte && grep "w-16.*__select__" anotame-web/src/lib/components/ui/DataTableWrapper.svelte | grep -c "w-16"</automated>
  </verify>
  <done>
    - Header and row checkboxes increased from 16x16px to 32x32px
    - Checkbox areas wrapped in 48x48px (h-12 w-12) clickable regions with negative margin for layout
    - Selection column width adjusted to accommodate larger touch targets
    - Checkbox layout centered within cells
    - Page still displays correctly with improved mobile touch targets
    - No visual regression in checkbox alignment
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Selection UI | User can select/deselect rows without explicit mode toggle |
| Selected rows → Bulk handlers | Handlers receive filtered set of selected Order objects from DataTableWrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Integrity | Bulk delete/status change | mitigate | Handlers validate selectedOrders array length before operations; confirmation dialogs show ticket numbers; deletion disabled unless all orders are RECEIVED status (line 38, allSelectedDeletable guard) |
| T-quick-02 | Information Disclosure | Selection state persistence | accept | Selection only persists within current page session; cleared on data refresh; low-risk tenant data |

</threat_model>

<verification>
**Manual verification steps:**

1. Navigate to `/dashboard/orders` (active tab)
2. Verify "Seleccionar pedidos" button is gone
3. Click any checkbox in a row → selection column should appear, FloatingActionBar should appear
4. Verify "Limpiar selección (1)" button appears above table when 1 item selected
5. Click "Cancelar" in FloatingActionBar or "Limpiar selección" button → selection should clear
6. Select multiple rows across pages:
   - Select rows on page 1
   - Click "Siguiente" to go to page 2
   - Verify rows from page 1 still show as selected
   - Select additional rows on page 2
   - FloatingActionBar count shows all selected
7. Test bulk delete: Select RECEIVED status orders, click delete, confirm dialog, verify deletion succeeds
8. Test bulk status change: Select orders, click status button, verify all are updated
9. **Mobile touch test:** On mobile device or browser dev tools mobile view, tap checkbox — must register touch reliably with 32x32px size

</verification>

<success_criteria>
- [ ] Orders page loads without "Seleccionar pedidos" toggle button visible
- [ ] Clicking a checkbox shows selection column and FloatingActionBar
- [ ] "Limpiar selección" button appears only when selectedOrders.length > 0
- [ ] Bulk delete, bulk status change, and clear selection all work without errors
- [ ] No TypeScript/svelte-check errors in modified files
- [ ] Checkbox touch targets verified at 32x32px or larger
- [ ] Selection persists correctly across pagination
- [ ] Mobile view shows touch-friendly checkbox targets
</success_criteria>

<output>
After successful execution:
1. Create `.planning/quick/260416-iu2-replace-orders-page-selecting-with-shadc/260416-iu2-SUMMARY.md`
2. Include: Files modified, tests passed, verification results, any issues encountered
3. Commit: `feat(260416-iu2): replace Orders page bulk selection with shadcn-svelte row actions pattern`
</output>
