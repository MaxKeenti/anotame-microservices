---
quick_id: 260410-svc5
slug: fix-price-list-clone-error
type: execute
must_haves:
  truths:
    - "Cloning an existing price list no longer throws 'props_invalid_value' in Svelte 5"
    - "PriceList form fields use standard Form.Control with snippet-based prop-passing"
    - "Cloned values (priority, active) are protected against null/undefined from API"
  artifacts:
    - path: "anotame-web/src/routes/(app)/dashboard/catalog/pricelists/new/+page.svelte"
      provides: "Fixed PriceList creation page with proper Svelte 5 FormSnap wiring"
---

# Quick Task: Fix Price List Clone Error

**Goal:** Resolve the `props_invalid_value` error during price list cloning by standardizing the `Form.Field` / `Form.Control` wiring and ensuring state updates use safe fallbacks.

## Tasks

### Task 1: Standardize Form Field Wiring and Protect State Updates

**Files:**
- `anotame-web/src/routes/(app)/dashboard/catalog/pricelists/new/+page.svelte`

**Action:**
1.  In `handleBaseListChange`, add fallbacks for `priority` (default 0) and `active` (default true) to prevent `null` from being set in the form state.
2.  Update `validFrom`, `validTo`, `active`, and `baseListId` fields to use the standard `Form.Control` wrap with `{#snippet children({ props })}`.
3.  Ensure `AdaptiveDatePicker` and `AdaptiveSelect` receive the `props` from `Form.Control` to correctly wire up accessibility and state interaction.
4.  Rename shadowed `props` identifier in snippets to `controlProps` to avoid potential confusion/collision in Svelte 5.

**Verify:**
- Manual verification: Cloning a list (via URL param) should load data into all fields without console errors.
- Code check: All `Form.Field` components should have a nested `Form.Control` component.

<output>
Create .planning/quick/260410-svc5-fix-price-list-clone-error/260410-svc5-SUMMARY.md after execution.
</output>
