---
phase: fix-svelte-check
plan: 260405-s7x
type: execute
wave: 1
depends_on: []
files_modified: [
  "anotame-web/src/lib/components/orders/wizard/items-step.svelte",
  "anotame-web/src/routes/(app)/dashboard/admin/settings/+page.svelte"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "svelte-check passes without errors in the modified files"
  artifacts:
    - path: "anotame-web/src/lib/components/orders/wizard/items-step.svelte"
      provides: "Type-safe reduce and map operations"
    - path: "anotame-web/src/routes/(app)/dashboard/admin/settings/+page.svelte"
      provides: "Correct type assignment for fontFamily in settings"
  key_links: []
---

<objective>
Fix 6 svelte-check diagnostics related to implicit 'any' types in `items-step.svelte` and a type mismatch in `settings/+page.svelte`.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Library/Mobile Documents/com~apple~CloudDocs/source/personal/anotame-microservices/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@anotame-web/src/lib/components/orders/wizard/items-step.svelte
@anotame-web/src/routes/(app)/dashboard/admin/settings/+page.svelte
@anotame-web/src/lib/services/orders/OrderWizardState.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix implicit 'any' types in items-step.svelte</name>
  <files>anotame-web/src/lib/components/orders/wizard/items-step.svelte</files>
  <action>
    - Import `DraftOrderItem` from `$lib/services/orders/OrderWizardState.svelte`.
    - Update the `$derived(items.reduce(...))` block (Line 16-17):
      - Explicitly type `acc` as `number`.
      - Explicitly type `item` as `DraftOrderItem`.
      - Explicitly type `sAcc` as `number`.
      - Explicitly type `s` in the inner reduce.
    - Update the `map` operation in `handleDuplicateItem` (Line 53):
      - Explicitly type the parameter `s` in the map callback.
    - Note: The current `items` variable is derived from `draft?.items || []`. Ensure `items` is correctly typed (e.g., `[] as DraftOrderItem[]`) to help inference if needed.
  </action>
  <verify>
    <automated>cd anotame-web && bun run svelte-check --threshold error --file src/lib/components/orders/wizard/items-step.svelte</automated>
  </verify>
  <done>Implicit 'any' errors are removed from items-step.svelte</done>
</task>

<task type="auto">
  <name>Task 2: Fix type mismatch in settings +page.svelte</name>
  <files>anotame-web/src/routes/(app)/dashboard/admin/settings/+page.svelte</files>
  <action>
    - Locate the `onValueChange` handler for the `fontFamily` Select component (approx. Line 254).
    - Cast the value being assigned to `$form.fontFamily` to match the expected enum type from the Zod schema.
    - Preferred fix: `$form.fontFamily = (v || '') as "Inter" | "Outfit" | "Merriweather" | "";` or use a type guard.
  </action>
  <verify>
    <automated>cd anotame-web && bun run svelte-check --threshold error --file src/routes/(app)/dashboard/admin/settings/+page.svelte</automated>
  </verify>
  <done>Type mismatch error is resolved in settings/+page.svelte</done>
</task>

<task type="auto">
  <name>Task 3: Full svelte-check verification</name>
  <files></files>
  <action>
    Run a full svelte-check on the `anotame-web` workspace to ensure no regressions were introduced and all 6 errors are gone.
  </action>
  <verify>
    <automated>cd anotame-web && bun run svelte-check</automated>
  </verify>
  <done>svelte-check returns 0 errors</done>
</task>

</tasks>

<verification>
Check that `bun run svelte-check` in `anotame-web` directory reports 0 errors.
</verification>

<success_criteria>
- All 6 specific errors identified are resolved.
- No new TypeScript or Svelte errors are introduced.
- `items-step.svelte` has proper typing for its reduction and mapping logic.
- `settings/+page.svelte` correctly handles the font selection type.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-s7x-fix-svelte-check-diagnostics-implicit-an/260405-s7x-SUMMARY.md`
</output>
