/**
 * Glass pill shared by the bottom dock and the bulk-action bar that replaces it,
 * so swapping one for the other keeps the same surface, radius, and height.
 */
export const DOCK_SURFACE =
	'pointer-events-auto flex h-16 max-w-[calc(100vw-2rem)] gap-2 rounded-3xl border border-border/40 bg-background/70 px-3.5 shadow-2xl shadow-black/15 backdrop-blur-2xl backdrop-saturate-150 sm:h-18';
