@model IEnumerable<Anotame.Models.Clientes>

@{
    Layout = null;
    ViewBag.Title = "Index";
}

@*<h2>Index</h2>*@

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@

<div class="row">
    <div class="col-md-5">
        <h2>Índice de clientes</h2>
    </div>
    <div class="col-md-7 row">
        <label class="control-label col-md-6">Buscar cliente:</label>
        <input placeholder="Escribe el nombre" class="form-control col-md-6" id="BusqClienT" />
    </div>
</div>

<table class="table" id="tblCliente">

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th>
                Editar
            </th>
            <th>
                Eliminar
            </th>
        </tr>
    </thead>

    <tbody>

        @foreach (var item in Model)
        {


            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Direccion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefono)
                </td>
                <td>
                    @{
                        var editUrl = Url.Action("Edit", new { id = item.id });
                        var deleteUrl = Url.Action("Delete", new { id = item.id });
                    }
                    @*@Html.ActionLink("Editar", "Edit", new { id = item.id })*@
                    <button type="button" class="btn btn-primary nticon btnEditCln" data-url="@editUrl">
                        <i id="icoEdit" class="fas fa-pen notaicon fa-2x"></i>
                    </button>
                </td>
                <td>
                    @*@Html.ActionLink("Details", "Details", new { id=item.id }) |
                        @Html.ActionLink("Eliminar", "Delete", new { id = item.id })*@
                    <button type="button" class="btn btn-primary nticon btnDeleteCln" data-url="@deleteUrl">
                        <i id="icoDelete" class="fas fa-trash notaicon fa-2x"></i>
                    </button>
                </td>
            </tr>
        }

    </tbody>

</table>
