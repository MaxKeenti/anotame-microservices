# Defaults shared by every service; a service's application.properties still overrides them.
# Prefix every log line with the X-Request-Id shared with the anotame-web proxy (see RequestIdFilter).
quarkus.log.console.format=%d{HH:mm:ss,SSS} %-5p [req=%X{requestId}] [%c{3.}] (%t) %s%e%n
