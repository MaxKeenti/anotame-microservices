# Workflow

This repository no longer keeps local GSD runtime bundles or generated planning state in source control.

Historical GSD context for this project lives outside the repo at:

```text
/Users/moonstone/.gsd/projects/b45d29e5a30d
```

Use that directory only as a read-only archive when reconstructing past milestone or slice context. A curated repo-local extract lives in [archive/gsd-merged-findings.md](./archive/gsd-merged-findings.md).

For new work, prefer a lightweight discussion-first workflow: clarify the decision, inspect the code when the answer is discoverable, and record only durable project language or hard-to-reverse decisions in the canonical docs listed in [docs/README.md](./README.md).
